# Task 15.2: Prueba end-to-end de Phase 15

## Execution Header
You are working in the existing repository. Implement only Task 15.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical web shell and
  dependency installer.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 15: Producto UI web con SvelteKit y workspaces embebidos

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar de forma end-to-end el shell web principal en una maquina real,
incluyendo el arranque del producto, la navegacion base y el puente con el
contrato canonico del repo.

## Minimal Context
La fase `15` queda completa solo cuando exista evidencia real de que:

- la app `SvelteKit` arranca por la ruta canonica del repo
- el workspace principal carga y navega
- la UI puede leer o invocar el contrato canonico sin crear un bridge paralelo
- las superficies embebidas o reservadas para herramientas futuras no rompen
  la app

Los tests unitarios o checks estaticos no sustituyen esta prueba.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/15.1-sveltekit-product-shell-and-embedded-workspaces.md`
- `docs/devplan/tasks/15.1.1-phase15-dependency-installation.md`
- `docs/architecture/runner-interface.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `scripts/apps/install-ui-web-deps.sh`

## Existing Infrastructure to Reuse
- Existing dependency installer from phase `15`: `scripts/apps/install-ui-web-deps.sh`
- Existing bootstrap hook: `scripts/bootstrap/apply-workstation.sh`
- Existing runner contract: `docs/architecture/runner-interface.md`
- Existing filesystem layouts under `STUDIO_DIR`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the dependency route from Task `15.1.1`.
- Reuse existing repo-wide backend installers for `ComfyUI`, `Kimodo`, and
  other tools only when the shell proof truly needs them.

## Upstream Validation Gate
- Phase `15` starts the UI pipeline and has no prior UI gate.
- Reuse only canonical repo invariants, the runner contract, and validated foundations from phases `0-14`.
- Execute this proof only after Tasks `15.1` and `15.1.1` are implementation-complete.
## Do Not Create
- Do not replace a real browser proof with only mocked route tests.
- Do not create a parallel proof server outside the canonical app path.
- Do not treat a shell that boots but cannot reach canonical repo state as a
  completed e2e proof.

## Required Change
- Provision the web shell dependencies using the repo-supported path.
- Run the product app in a real browser-backed e2e flow.
- Exercise the shell landing experience and at least one route that consumes
  canonical repo state or the runner contract.
- Publish the real outcome of the proof and document blockers if any exist.
- Update the canonical docs only if the proof is truly complete.

## Deliverables
- real e2e evidence for the product shell
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/15.2-phase15-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/15.2-phase15-e2e-proof.md`
- `docs/SAD.md` only if the proof reveals an architecture-level correction

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
npm --prefix apps/openclaw-ui run lint
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test
```

### Integration / smoke validation
```bash
bash scripts/apps/install-ui-web-deps.sh apply
npm --prefix apps/openclaw-ui run dev -- --host 127.0.0.1 --port 4173
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase15-shell
```

## Expected Evidence

* Expected output file: browser-backed e2e evidence for the shell routes
* Expected log/result file: `Playwright` report or equivalent plus the route
  or adapter diagnostics used in the proof
* Expected status: real `pass` for the shell proof, or explicit blocker with
  diagnosis
* Expected exit code: `0` for the accepted proof flow

## Acceptance Criteria

* The required change is implemented.
* Existing dependency and runner infrastructure is reused.
* No duplicate proof path is introduced.
* The shell is proven through a real browser-backed run.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.

## Proof Outcome (2026-05-14)

* real proof status: `pass`
* browser-backed command:
  `npm --prefix apps/openclaw-ui run test:e2e -- --grep phase15-shell`
* validated shell surfaces:
  `/`, `/workspaces/comfyui`, `/workspaces/kimodo`, `/workspaces/kimodo/embed`,
  and `/api/runners`
* dependency route used:
  `bash scripts/apps/install-ui-web-deps.sh apply`
* parallel relevant actions requested from other tasks:
  - Task `1.2`: `scripts/hardening/check-user.sh` -> runtime user still in
    `sudo` and `adm` (phase `1` remains pending, non-blocking for this UI proof)
  - Task `9.11.3`: `scripts/apps/comfyui.sh status` and
    `scripts/apps/blender.sh status` plus SF3D workflow file presence checks -> pass
