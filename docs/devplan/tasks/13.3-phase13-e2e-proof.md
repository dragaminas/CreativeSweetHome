# Task 13.3: Prueba end-to-end de Phase 13

## Execution Header
You are working in the existing repository. Implement only Task 13.3.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 13: Cleanup 3D pre-rigging con Blender e Instant Meshes

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar la fase `13` de forma end-to-end en una maquina Linux real usando la
ruta canonica completa:
`dependency install -> runner start -> Blender cleanup -> Instant Meshes -> evidence review`

## Minimal Context
La infraestructura de la fase `13` ya existe:

- runner canonico `blender`
- helper de cleanup en `Blender`
- wrapper de `Instant Meshes`
- script de instalacion y audit de dependencias de host

Lo que falta no es arquitectura nueva, sino prueba operativa real. Hasta
ahora el repo solo ha validado:

- checks estaticos
- tests unitarios con wrappers falsos
- auditoria de dependencias

Todavia no existe una corrida real aceptada con:

- dependencias realmente instaladas
- un modelo humanoide real de entrada
- evidencia publicada bajo `Assets3D/.../cleanup/<run_id>/`
- salida remesheada de `Instant Meshes`

Esta tarea debe cerrar ese gap con evidencia revisable y dejar documentado el
resultado real, incluyendo cualquier bloqueo o degradacion observada.

## Scope Budget (Roo/Qwen)
- Primary surface: `e2e_proof`
- Target changed files (soft cap): `6`
- Hard cap (must stop and split): `8`
- Max code areas: `1 flujo` (`proof run + evidencia + task docs`)
- Out-of-scope guardrail: no refactor de arquitectura ni nuevos contratos; solo prueba real y evidencia.
- Split trigger: si cerrar la task supera el soft cap (`6`) o expande areas no previstas, detener, abrir task hermana y replanificar antes de seguir.
- Overflow protocol: al alcanzar hard cap (`8`), detener implementacion, marcar status `blocked_scope_split_required`, registrar gap tecnico y enlazar task hermana en `docs/devplan/01-phase-index.md`.

## Files to Inspect First
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/13.1-blender-instant-meshes-pre-rigging-cleanup.md`
- `docs/devplan/tasks/13.2-phase13-dependency-installation.md`
- `docs/operations/3d-pre-rig-cleanup.md`
- `scripts/apps/install-3d-pre-rig-deps.sh`
- `scripts/actions/runner-action.sh`
- `scripts/apps/blender.sh`
- `scripts/apps/instant-meshes.sh`
- `src/openclaw_studio/runners/blender.py`
- `tests/test_blender_runner.py`

## Existing Infrastructure to Reuse
- Existing dependency installer: `scripts/apps/install-3d-pre-rig-deps.sh`
- Existing bootstrap hook: `scripts/bootstrap/apply-workstation.sh`
- Existing runner contract and CLI bridge: `src/openclaw_studio/runners/contracts.py`, `src/openclaw_studio/runner_cli.py`, `scripts/actions/runner-action.sh`
- Existing Blender cleanup helper: `scripts/apps/blender_pre_rig_cleanup.py`
- Existing evidence layout under `$STUDIO_DIR/Assets3D/<project>/<entity_id>/cleanup/<run_id>/`
- Existing canonical statuses and reports from phase `13`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical dependency path from Task `13.2` via `scripts/apps/install-3d-pre-rig-deps.sh`.
- Reuse the existing bootstrap hook in `scripts/bootstrap/apply-workstation.sh`.

## Source of Truth Matrix
- Domain: `task_status`.
- Authoritative Source: `docs/devplan/tasks/13.3-phase13-e2e-proof.md` (`## Status` y `Implementation Outcome` cuando aplique).
- Derived/Projection Artifacts: `docs/devplan/task-status-index.md`, `docs/devplan/01-phase-index.md`.
- Reconciliation Command: `python3 scripts/devplan/build-task-status-index.py --write`.
- Write Rule: editar primero esta task file y luego regenerar el indice de estado.
- Domain: `runtime_state`.
- Authoritative Source: artefactos y manifiestos canonicos declarados en esta task (`scripts/`, `docs/operations/`, evidencia de runner).
- Derived/Projection Artifacts: resumenes de fase y estado (`docs/devplan/01-phase-index.md`, `docs/devplan/task-status-index.md`).
- Reconciliation Command: `scripts/devplan/check-task-files.sh`.
- Write Rule: actualizar primero artefacto operativo canonico; luego sincronizar documentacion y estado derivado.
- Domain: `validation_evidence`.
- Authoritative Source: rutas de `Expected Evidence` y salidas reales de comandos en `Verification`.
- Derived/Projection Artifacts: notas de cierre, resumenes de fase y feedback de UI.
- Reconciliation Command: ejecutar todos los comandos de `Verification` de esta task sin atajos.
- Write Rule: no marcar `done` si la evidencia no existe o no es trazable a rutas canonicas.

## Implementation Contract (No-Drift)
- Primary authority boundary: escenario e2e real de fase y recoleccion de evidencia en rutas canonicas.
- Explicit non-authority boundary: no reemplazar prueba real por mocks/unit-only ni publicar evidencia fuera de rutas canonicas.
- Required inputs (existing artifacts): `Files to Inspect First` + gate upstream declarado + docs canonicos referenciados.
- Required outputs (exact paths): `Deliverables` + evidencia de `Expected Evidence`.
- Sequencing constraints: preparar prerequisitos reales de fase; ejecutar flujo completo sin atajos; capturar evidencia en disco; registrar resultado con estado explicito.
- Failure policy: ante bloqueo real, devolver `blocked_*`/`fail_*` con causa tecnica, comando ejecutado y ruta afectada; no inventar rutas paralelas.
- Evidence policy: cada claim de avance debe mapear a archivo/log revisable en repo o `STUDIO_DIR`.

## Detailed Execution Context (Anti-Invention)
- Canonical authority model: la autoridad operativa esta en artefactos canonicos de scripts/runners declarados por la fase.
- Mandatory guardrails: reutilizar infraestructura existente, no duplicar contratos, no abrir taxonomias de estado paralelas.
- Gate discipline: si la task define `Upstream Validation Gate`, no iniciar implementacion hasta satisfacerlo.
- Evidence discipline: la evidencia debe quedar en rutas revisables y mencionarse explicitamente en `Implementation Outcome`.
- Drift control: si aparece una necesidad fuera de alcance, detener y crear task hermana en lugar de improvisar expansion.
- Roo/Codex handoff note: ejecutar exactamente las microtasks listadas; cualquier desviacion requiere justificar impacto y actualizar esta task file antes de codificar.
- Context note: La prueba debe ejercer flujo completo de usuario/sistema y fallar si falta evidencia o prerequisito.

## Do Not Create
- Do not create a second proof path outside the canonical dependency installer and runner entrypoint.
- Do not create a parallel fixture runner, shell bridge, or evidence root just for validation.
- Do not replace the real end-to-end run with only mocked or fake-wrapper tests.
- Do not silently downgrade a failed remesh to a claimed end-to-end success.

## Required Change
- Install or provision the phase `13` dependencies through the repo-supported path.
- Select or prepare a representative humanoid source model suitable for the phase goal.
- Run the canonical `blender` runner against that real input using the phase `13` entrypoint.
- Publish and review the resulting evidence tree, including before/after refs, logs, manifests, and cleanup report.
- Record the real outcome of the run:
  `pass` or explicit `blocked_*`/`fail_*` with diagnosis.
- Update the canonical docs so phase `13` is marked `done` only if the end-to-end proof is truly complete.

## Microtask Breakdown
- [ ] MT1: Verificar prerequisitos duros y preparar input real de la corrida e2e. files: `docs/devplan/tasks/13.3-phase13-e2e-proof.md`, `$STUDIO_DIR/Assets3D/<project>/<entity_id>/cleanup/<run_id>/`. verify: `bash -n scripts/apps/install-3d-pre-rig-deps.sh`.
- [ ] MT2: Ejecutar la corrida e2e canonica completa sin atajos con mocks. files: `$STUDIO_DIR/Assets3D/<project>/<entity_id>/cleanup/<run_id>/`, `tests/test_blender_runner.py`. verify: `bash scripts/apps/install-3d-pre-rig-deps.sh apply`.
- [ ] MT3: Registrar evidencia, estado final y bloqueos explicitos (`pass` o `blocked_*`/`fail_*`). files: `docs/devplan/tasks/13.3-phase13-e2e-proof.md`, `docs/devplan/01-phase-index.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- real evidence under `$STUDIO_DIR/Assets3D/<project>/<entity_id>/cleanup/<run_id>/`
- `tests/test_blender_runner.py`
- `docs/devplan/tasks/13.3-phase13-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/13.3-phase13-e2e-proof.md`
- `docs/operations/3d-pre-rig-cleanup.md` if the real run changes the documented operator path
- `docs/SAD.md` only if the proof reveals an architecture-level correction

## Verification

### Static checks
```bash
bash -n scripts/apps/install-3d-pre-rig-deps.sh
bash -n scripts/actions/runner-action.sh
bash -n scripts/apps/blender.sh
bash -n scripts/apps/instant-meshes.sh
python3 -m compileall src tests
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests.test_blender_runner -v
```

### Integration / smoke validation
```bash
INSTANT_MESHES_BIN='/abs/path/to/Instant Meshes' bash scripts/apps/install-3d-pre-rig-deps.sh apply
scripts/apps/blender.sh status
INSTANT_MESHES_BIN='/abs/path/to/Instant Meshes' scripts/apps/instant-meshes.sh status
scripts/actions/runner-action.sh describe blender
scripts/actions/runner-action.sh list-targets blender operate
INSTANT_MESHES_BIN='/abs/path/to/Instant Meshes' \
OPENCLAW_RUNNER_INPUTS_JSON='{"source_model_path":"/abs/path/to/humanoid.glb","project_id":"phase13-e2e","entity_id":"humanoid-e2e"}' \
OPENCLAW_RUNNER_OPTIONS_JSON='{"mode":"auto"}' \
scripts/actions/runner-action.sh start blender operate cleanup_pre_rig_humanoid
```

## Expected Evidence

* Expected output file: cleaned export and remeshed output under `$STUDIO_DIR/Assets3D/phase13-e2e/humanoid-e2e/cleanup/<run_id>/`
* Expected log/result file: `manifests/run.json`, `manifests/summary.json`, backend logs, and `cleanup-report.md`
* Expected status: real `pass` for the representative e2e proof, or explicit documented blocker/failure if the host or asset prevents that result
* Expected exit code: `0` for accepted queries and the supported proof flow

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* The dependencies are proven through the repo-supported install path, not just audit mode.
* A real humanoid asset is processed through the canonical phase `13` runner.
* The evidence tree is published and reviewable.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.

## Implementation Outcome (2026-05-17)

* implementation status: `done`
* proof run status: `pass`
* representative humanoid input:
  `/home/eric/Studio/Assets3D/phase13-e2e/humanoid-e2e/input/humanoid-source-cesiumman.glb`
* dependency provisioning command used:
  `INSTANT_MESHES_BIN='/home/eric/instant-meshes-prebuilt/Instant Meshes' bash scripts/apps/install-3d-pre-rig-deps.sh apply`
* canonical e2e run command used:
  `INSTANT_MESHES_BIN='/home/eric/instant-meshes-prebuilt/Instant Meshes' OPENCLAW_RUNNER_INPUTS_JSON='{"source_model_path":"/home/eric/Studio/Assets3D/phase13-e2e/humanoid-e2e/input/humanoid-source-cesiumman.glb","project_id":"phase13-e2e","entity_id":"humanoid-e2e"}' OPENCLAW_RUNNER_OPTIONS_JSON='{"mode":"auto"}' scripts/actions/runner-action.sh start blender operate cleanup_pre_rig_humanoid`
* accepted run id: `cleanup-20260517-144129`
* evidence root:
  `/home/eric/Studio/Assets3D/phase13-e2e/humanoid-e2e/cleanup/cleanup-20260517-144129`
* verified evidence files:
  - `manifests/run.json`
  - `manifests/summary.json`
  - `manifests/blender-cleanup.json`
  - `logs/blender.stdout.log`
  - `logs/blender.stderr.log`
  - `logs/instant-meshes.stdout.log`
  - `logs/instant-meshes.stderr.log`
  - `cleanup-report.md`
  - `output/humanoid-e2e__cleaned__v001.obj`
  - `output/humanoid-e2e__cleaned__v001.glb`
  - `output/humanoid-e2e__remeshed__v001.obj`
* backend exits in accepted run:
  - `blender`: `0`
  - `instant_meshes`: `0`
* run warning captured in evidence:
  - `multiple mesh objects remain after cleanup`
* TDD fix shipped during proof:
  - `src/openclaw_studio/runners/blender.py`: backend command execution now captures stdout/stderr via `PIPE` before persisting logs, avoiding false `exit 120` failures seen when Blender output is redirected directly to files.
  - `src/openclaw_studio/runners/blender.py`: `artifact_refs` now includes manifests and backend logs.
  - `scripts/apps/install-3d-pre-rig-deps.sh`: `apply` now skips host build-prereq installation and source checkout/build when an executable `INSTANT_MESHES_BIN` is already available.
  - `tests/test_blender_runner.py`: new regression coverage for both fixes.
