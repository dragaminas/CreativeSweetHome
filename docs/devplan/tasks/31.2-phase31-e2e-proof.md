# Task 31.2: Prueba end-to-end de Phase 31

## Execution Header
You are working in the existing repository. Implement only Task 31.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical Resolve-assembly flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 31: Montaje automatizado de tomas en DaVinci Resolve

## Status
`pending`

## Goal
Probar en una corrida real el montaje automatizado de tomas desde la UI hasta
la publicacion de estado y evidencia revisables.

## Minimal Context
La fase se cierra cuando una escena con shots generados puede llegar al paso de
assembly desde el producto web y producir un resultado real o un bloqueo
explcito y reproducible.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/31.1-ui-davinci-resolve-automated-assembly.md`
- `docs/devplan/tasks/31.1.1-phase31-dependency-installation.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing shot outputs from phases `28-30`
- Existing product shell from phase `15`
- Existing Resolve dependency path from Task `31.1.1`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the canonical Resolve-readiness path from Task `31.1.1`.

## Upstream Validation Gate
- Start only after Task `30.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated generated shot outputs produced by phase `30`.
- Execute this proof only after the implementation tasks of phase `31` and Task `31.0`, Task `31.1.1`, and the automated-assembly UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not publish assembly evidence outside canonical repo-owned paths.

## Required Change
- Run a real browser-backed Resolve assembly flow.
- Verify resulting state, output refs, or explicit blockers under canonical paths.
- Publish the real outcome of the proof.

## Deliverables
- real Resolve-assembly proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/31.2-phase31-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/31.2-phase31-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- resolve-assembly
```

### Integration / smoke validation
```bash
bash scripts/apps/install-davinci-resolve-deps.sh audit
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase31-resolve-assembly
```

## Expected Evidence

* Expected output file: scene-assembly state or output refs in canonical
  repo-owned paths
* Expected log/result file: e2e report plus real assembly output or explicit
  blocked evidence
* Expected status: real `pass` for Resolve assembly or explicit blocker
* Expected exit code: `0` for supported proof flows

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or evidence path is introduced.
* A real Resolve assembly flow is exercised through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
