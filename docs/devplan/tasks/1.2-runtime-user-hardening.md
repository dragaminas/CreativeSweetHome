# Task 1.2: Hardening del usuario runtime

## Execution Header
You are working in the existing repository. Implement only Task 1.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 1: Hardening del sistema anfitrion

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar el hardening del usuario runtime para que el stack opere sin grupos
sensibles y con evidencia documental verificable.

## Minimal Context
El repo ya tiene checks de usuario, discos y GNOME, y el bootstrap ya ejecuta
validaciones de hardening. El gap real pendiente es `1.2`: documentar y cerrar
la remocion del usuario runtime de grupos como `sudo` y `adm`, validando el
resultado con la infraestructura ya existente.

Ya existe:

- `scripts/hardening/check-user.sh`
- `scripts/bootstrap/apply-workstation.sh`
- documentacion de discos y automontaje

Debe seguir siendo compatible:

- el bootstrap `audit/apply`
- la configuracion por `.env`
- los servicios `systemd --user`

Fuera de alcance:

- redisenar el modelo de identidad del repo
- introducir un nuevo sistema de provision de usuarios

## Scope Budget (Roo/Qwen)
- Primary surface: `backend_contract`
- Target changed files (soft cap): `6`
- Hard cap (must stop and split): `8`
- Max code areas: `2 modulos` (`runner/backend contract` + `tests/docs canonicos`)
- Out-of-scope guardrail: no tocar rutas UI ni reabrir otras fases; no agregar runners paralelos.
- Split trigger: si cerrar la task supera el soft cap (`6`) o expande areas no previstas, detener, abrir task hermana y replanificar antes de seguir.
- Overflow protocol: al alcanzar hard cap (`8`), detener implementacion, marcar status `blocked_scope_split_required`, registrar gap tecnico y enlazar task hermana en `docs/devplan/01-phase-index.md`.

## Files to Inspect First
- `scripts/hardening/check-user.sh`
- `scripts/bootstrap/apply-workstation.sh`
- `.env.example`
- `docs/security/disks-and-automount.md`
- `docs/operations/bootstrap.md`

## Existing Infrastructure to Reuse
- Existing validation script: `scripts/hardening/check-user.sh`
- Existing bootstrap entrypoint: `scripts/bootstrap/apply-workstation.sh`
- Existing config source: `.env.example`
- Existing diagnostics: `scripts/doctor/openclaw-status.sh`
- Existing evidence convention: documentacion operativa bajo `docs/security/`

## Dependency Provisioning
- Esta tarea no introduce nuevas dependencias del host ni paquetes del runtime.
- Reutiliza la ruta canonica de bootstrap existente (`audit/apply`) y el check
  de usuario ya presente en `scripts/hardening/check-user.sh`.

## Source of Truth Matrix
- Domain: `task_status`.
- Authoritative Source: `docs/devplan/tasks/1.2-runtime-user-hardening.md` (`## Status` y `Implementation Outcome` cuando aplique).
- Derived/Projection Artifacts: `docs/devplan/task-status-index.md`, `docs/devplan/01-phase-index.md`.
- Reconciliation Command: `python3 scripts/devplan/build-task-status-index.py --write`.
- Write Rule: editar primero esta task file y luego regenerar el indice de estado.
- Domain: `runtime_state`.
- Authoritative Source: artefactos y manifiestos canonicos declarados en esta task (`scripts/`, `docs/operations/`, evidencia de runner).
- Derived/Projection Artifacts: resumenes de fase y estado (`docs/devplan/01-phase-index.md`, `docs/devplan/task-status-index.md`).
- Reconciliation Command: `scripts/devplan/check-task-files.sh`.
- Write Rule: actualizar primero artefacto operativo canonico; luego sincronizar documentacion y estado derivado.
- Domain: `validation_evidence`.
- Authoritative Source: rutas de `Expected Evidence` y salidas reales de comandos en `Verification`.
- Derived/Projection Artifacts: notas de cierre, resumenes de fase y feedback de UI.
- Reconciliation Command: ejecutar todos los comandos de `Verification` de esta task sin atajos.
- Write Rule: no marcar `done` si la evidencia no existe o no es trazable a rutas canonicas.

## Implementation Contract (No-Drift)
- Primary authority boundary: slice operativo acotado declarado por la task.
- Explicit non-authority boundary: no introducir infraestructura nueva fuera del alcance declarado.
- Required inputs (existing artifacts): `Files to Inspect First` + gate upstream declarado + docs canonicos referenciados.
- Required outputs (exact paths): `Deliverables` + evidencia de `Expected Evidence`.
- Sequencing constraints: aplicar microtasks en orden, con verificacion tras cada slice y sin expandir superficie.
- Failure policy: ante bloqueo real, devolver `blocked_*`/`fail_*` con causa tecnica, comando ejecutado y ruta afectada; no inventar rutas paralelas.
- Evidence policy: cada claim de avance debe mapear a archivo/log revisable en repo o `STUDIO_DIR`.

## Detailed Execution Context (Anti-Invention)
- Canonical authority model: la autoridad operativa esta en artefactos canonicos de scripts/runners declarados por la fase.
- Mandatory guardrails: reutilizar infraestructura existente, no duplicar contratos, no abrir taxonomias de estado paralelas.
- Gate discipline: si la task define `Upstream Validation Gate`, no iniciar implementacion hasta satisfacerlo.
- Evidence discipline: la evidencia debe quedar en rutas revisables y mencionarse explicitamente en `Implementation Outcome`.
- Drift control: si aparece una necesidad fuera de alcance, detener y crear task hermana en lugar de improvisar expansion.
- Roo/Codex handoff note: ejecutar exactamente las microtasks listadas; cualquier desviacion requiere justificar impacto y actualizar esta task file antes de codificar.
- Context note: La implementacion debe mantenerse en el budget y splittear task si excede superficie.

## Do Not Create
- Do not create a parallel user-hardening script.
- Do not create a second bootstrap path outside `apply-workstation.sh`.
- Do not create a separate status format for this check.
- Do not create a second document for disks or GNOME when `docs/security/` already exists.

## Required Change
- Definir y documentar el procedimiento soportado para operar con separacion
  `admin + runtime`: la cuenta admin puede conservar `sudo`, mientras el
  usuario runtime queda fuera de grupos sensibles.
- Reutilizar `check-user.sh` y el bootstrap para validar el estado final.
- Crear la documentacion faltante de `1.2` sin duplicar lo ya cubierto por
  discos y automontaje.
- Dejar claro que la tarea solo cierra con evidencia real o con un bloqueo
  operativo explicitado.

## Microtask Breakdown
- [ ] MT1: Acotar contrato backend y puntos de integracion exactos a tocar. files: `docs/devplan/tasks/1.2-runtime-user-hardening.md`, `docs/security/runtime-user-hardening.md`. verify: `bash -n scripts/hardening/check-user.sh`.
- [ ] MT2: Implementar el slice minimo del backend sin duplicar runners/contratos. files: `docs/security/runtime-user-hardening.md`, `scripts/hardening/check-user.sh`. verify: `python3 -m compileall src tests`.
- [ ] MT3: Validar operacion canonica y cobertura de pruebas del flujo agregado. files: `scripts/hardening/check-user.sh`, `scripts/bootstrap/apply-workstation.sh`. verify: `scripts/hardening/check-user.sh`.
- [ ] MT4: Actualizar docs canonicos y cerrar gate con trazabilidad revisable. files: `docs/devplan/tasks/1.2-runtime-user-hardening.md`, `docs/SAD.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `docs/security/runtime-user-hardening.md`
- `scripts/hardening/check-user.sh`
- `scripts/bootstrap/apply-workstation.sh`
- `docs/operations/bootstrap.md`

## Canonical Docs to Update
- `docs/SAD.md` si cambia la lectura actual del hardening del host o del runtime
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md` si cambia el alcance de `FEAT-SEC-001`
- `docs/devplan/tasks/1.2-runtime-user-hardening.md`
- `docs/devplan/archive/phase-00-07-foundation-summary.md` cuando `1.2` deje de ser tarea activa

## Verification

### Static checks
```bash
bash -n scripts/hardening/check-user.sh
bash -n scripts/bootstrap/apply-workstation.sh
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
scripts/hardening/check-user.sh
scripts/bootstrap/apply-workstation.sh audit
scripts/doctor/openclaw-status.sh
scripts/devplan/check-task-files.sh
```

## Expected Evidence

* Expected output file: `docs/security/runtime-user-hardening.md`
* Expected log/result file: salida revisable de `scripts/hardening/check-user.sh`
* Expected status: usuario runtime sin pertenencia operativa a `sudo` ni `adm`, o bloqueo documentado con precision
* Expected exit code: `0` cuando el hardening este cerrado; salida bloqueante documentada si no puede completarse

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
