# Task 11.11.5: Rollback explicito de Trellis2 a Hunyuan3D

## Execution Header
You are working in the existing repository. Implement only Task 11.11.5.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 11: Reapertura 3D en ComfyUI con Trellis2 GGUF

## Status
`pending`

## Goal
Definir y dejar operativo un rollback explicito que permita volver
temporalmente a `Hunyuan3D` sin downtime de operacion.

## Minimal Context
Si Trellis2 llega a promocionarse, el sistema necesita una forma clara de
volver al baseline estable sin rehacer el catalogo ni romper aliases. El
rollback debe ser explicito y reversible.

Debe seguir siendo compatible:

- la continuidad de `Hunyuan3D`
- la seleccion actual de variantes
- la documentacion de fallback ya existente

Fuera de alcance:

- eliminar Trellis2 del repo
- volver a `SF3D` como rollback principal

## Files to Inspect First
- `.env.example`
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/hunyuan3d/fallback-paths.md`

## Existing Infrastructure to Reuse
- Existing stable runtime: `Hunyuan3D`
- Existing flow catalog: `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- Existing selector: `src/openclaw_studio/application/session_engine.py`
- Existing config source: `.env.example`

## Do Not Create
- Do not create a second config file just for rollback.
- Do not create a second alias set for the same `UC-3D-*`.
- Do not require manual surgery in the repo to roll back.

## Required Change
- Definir un mecanismo explicito y reversible para volver a `Hunyuan3D`.
- Reutilizar la configuracion o la seleccion ya existentes, evitando formatos
  paralelos.
- Documentar el procedimiento operativo y el efecto sobre la seleccion de
  variantes.

## Deliverables
- `.env.example`
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/hunyuan3d/fallback-paths.md`

## Canonical Docs to Update
- `docs/SAD.md` si el rollback cambia la lectura actual del baseline 3D
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/11.11.5-trellis2-rollback.md`
- `docs/devplan/archive/phase-10-hunyuan3d-summary.md` si el rollback redefine el baseline vigente

## Verification

### Static checks
```bash
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests.test_flow_catalog -v
scripts/devplan/check-task-files.sh
```

### Integration / smoke validation
```bash
PYTHONPATH=src python3 -m openclaw_studio
scripts/actions/runner-action.sh describe hunyuan3d
```

## Expected Evidence

* Expected output file: `docs/comfyui/trellis2-gguf-interface.md`
* Expected log/result file: procedimiento de rollback documentado y selector verificable
* Expected status: rollback reversible a `Hunyuan3D` sin downtime de operacion
* Expected exit code: `0` para verificaciones del catalogo y del runner estable

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
