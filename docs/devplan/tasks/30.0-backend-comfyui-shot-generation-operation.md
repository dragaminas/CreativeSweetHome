# Task 30.0: Backend canonico de generacion de tomas en ComfyUI

## Execution Header
You are working in the existing repository. Implement only Task 30.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate shot-generation pipelines, evidence roots, or raw
  ComfyUI product paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 30: Generacion de tomas con ComfyUI como engine

## Status
`pending`

## Goal
Cerrar la operacion backend canonica que genera tomas finales usando `ComfyUI`
como engine y el contrato canonico de runner.

## Minimal Context
La fase UI `30` necesita un backend de producto para renderizar tomas. El repo
ya tiene `ComfyUI`, workflows y validaciones, pero no una operacion canonica
para este paso.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/comfyui/interface.md`
- `docs/comfyui/general-video-render-workflow.md`
- `src/openclaw_studio/runners/comfyui.py`
- `docs/devplan/tasks/29.0-backend-comfyui-initial-image-operation.md`
- `docs/devplan/tasks/30.1-ui-shot-generation-with-comfyui-engine.md`

## Existing Infrastructure to Reuse
- Existing initial-image flow from phase `29`
- Existing `ComfyUI` runtime and runner contract
- Existing canonical evidence and status conventions
- Existing export and shot context from earlier phases

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `29.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated initial image and base-video package produced by phase `29`.
- The UI e2e for phase `30` must wait for this backend task to be complete.

## Do Not Create
- Do not expose the raw `ComfyUI` canvas as a substitute for this backend.
- Do not create a second render runner or evidence root.
- Do not force the user-facing phase to know backend graph details.

## Required Change
- Extend the canonical `comfyui` runner with a product-ready shot-generation
  operation.
- Accept structured product inputs and return canonical progress, artifact refs,
  evidence refs, cancellation state, and readable errors.
- Keep the operation reusable by the UI phase and later post-production phases.

## Deliverables
- `src/openclaw_studio/runners/comfyui.py`
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/30.0-backend-comfyui-shot-generation-operation.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests -v
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
scripts/actions/runner-action.sh list-targets comfyui operate
```

## Expected Evidence

* Expected output file: canonical generated-shot artifacts
* Expected log/result file: backend run evidence, progress state, and readable
  result payload
* Expected status: the repo can generate a shot through a canonical backend path
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `ComfyUI`, shot, and evidence infrastructure is reused.
* No duplicate render runner or evidence root is introduced.
* The backend operation is sufficient for the UI phase and later Resolve phases.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
