# Task 32.1: Refinamiento manual de escena en DaVinci Resolve asistido por UI

## Execution Header
You are working in the existing repository. Implement only Task 32.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate refine workflows, evidence roots, or product shells.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 32: Refinamiento manual de escena en DaVinci Resolve

## Status
`pending`

## Goal
Implementar el paso asistido de refinamiento manual de escena en `DaVinci Resolve`
dentro del shell de producto sin perder trazabilidad del pipeline.

## Minimal Context
Tras el assembly automatizado, la persona usuaria puede necesitar ajustes
manuales. La app debe aportar:

- contexto de escena
- checklist
- estado del pipeline
- rutas de salida

La app puede lanzar o asistir `Resolve`, pero no debe reimplementar toda su UI.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/31.1-ui-davinci-resolve-automated-assembly.md`
- `docs/devplan/tasks/31.1.1-phase31-dependency-installation.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing Resolve assembly flow from phase `31`
- Existing product shell from phase `15`
- Existing scene and export context from earlier phases

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the canonical Resolve-readiness path from Task `31.1.1`.

## Upstream Validation Gate
- Start only after Task `31.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated scene assembly state produced by phase `31`.
- Complete Task `32.0` before phase `32` is considered ready for e2e proof.
## Do Not Create
- Do not create a fake web reimplementation of `DaVinci Resolve`.
- Do not create a second refine-state model outside canonical repo-owned state.
- Do not create a new scene-evidence root outside the canonical pipeline.

## Required Change
- Add a manual-refinement workspace for `Resolve` in the product UI.
- Expose context, checklist, state, and routes to open or assist `Resolve`.
- Capture the return state of the refinement step and make it visible in the UI.
- Keep the handoff into final export explicit and reviewable.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `docs/devplan/UIPlan.md`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/32.1-ui-davinci-resolve-manual-scene-refinement.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- resolve-refine
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase32-resolve-refine
```

## Expected Evidence

* Expected output file: refine-state or updated scene artifact refs in canonical
  repo-owned paths
* Expected log/result file: e2e evidence showing context, checklist, and
  returned refinement state
* Expected status: the UI can assist manual scene refinement without becoming a
  second video editor
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing Resolve assembly and shell infrastructure is reused.
* No duplicate refine workflow or evidence root is introduced.
* The UI provides context and state for manual refinement without reimplementing
  `DaVinci Resolve`.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
