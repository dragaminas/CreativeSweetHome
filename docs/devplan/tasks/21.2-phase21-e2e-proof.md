# Task 21.2: Prueba end-to-end de Phase 21

## Execution Header
You are working in the existing repository. Implement only Task 21.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical cleanup flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 21: Cleanup automatizado de meshes en UI

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real el cleanup de un asset 3D desde la UI hasta la
publicacion de evidencia completa bajo el layout canonico de `Assets3D/`.

## Minimal Context
La fase solo se cierra cuando un asset real puede pasar por la UI, disparar el
runner canonico de cleanup y devolver evidencia revisable y feedback legible.

## Scope Budget (Roo/Qwen)
- Primary surface: `e2e_proof`
- Max changed files: `4`
- Max code areas: `1 flujo` (`proof run + evidencia + task docs`)
- Out-of-scope guardrail: no refactor de arquitectura ni nuevos contratos; solo prueba real y evidencia.
- Split trigger: si cerrar la task exige >`4` archivos o tocar otra fase, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/21.1-ui-mesh-cleanup-integration.md`
- `docs/operations/3d-pre-rig-cleanup.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/blender.py`

## Existing Infrastructure to Reuse
- Existing cleanup runner from phase `13`
- Existing UI shell from phase `15`
- Existing asset 3D flow from phase `20`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing phase `13` dependency path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `20.2` reports `pass` (hard gate).
- Reuse the validated 3D candidate produced by phase `20`.
- Execute this proof only after the implementation tasks of phase `21` and the mesh-cleanup integration is complete and phase `13` remains the reused backend path.
## Do Not Create
- Do not replace the proof with only mocked tests.
- Do not publish cleanup evidence outside the canonical runner layout.

## Required Change
- Run a real browser-backed cleanup flow from the product UI.
- Verify resulting before/after refs, report, and manifests under the canonical
  cleanup layout.
- Publish the real outcome of the proof.

## Microtask Breakdown
- [ ] MT1: Verificar prerequisitos duros y preparar input real de la corrida e2e. files: `docs/devplan/tasks/21.2-phase21-e2e-proof.md`, `real cleanup proof evidence linked from the product UI`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Ejecutar la corrida e2e canonica completa sin atajos con mocks. files: `real cleanup proof evidence linked from the product UI`, `apps/openclaw-ui/tests/e2e/shell.spec.ts`. verify: `scripts/actions/runner-action.sh describe blender`.
- [ ] MT3: Registrar evidencia, estado final y bloqueos explicitos (`pass` o `blocked_*`/`fail_*`). files: `docs/devplan/tasks/21.2-phase21-e2e-proof.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- real cleanup proof evidence linked from the product UI
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/21.2-phase21-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/21.2-phase21-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- mesh-cleanup
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe blender
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase21-mesh-cleanup
```

## Expected Evidence

* Expected output file: canonical cleanup evidence under `Assets3D/.../cleanup/`
* Expected log/result file: e2e report plus run artifacts, manifests, and
  cleanup report refs
* Expected status: real `pass` for cleanup integration or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing cleanup infrastructure is reused.
* No duplicate proof or evidence path is introduced.
* A real cleanup run is exercised through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
