# Task 20.0: Backend canonico de asset a 3D

## Execution Header
You are working in the existing repository. Implement only Task 20.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate 3D runtimes, evidence roots, or orchestration paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 20: Importacion o modelado 3D de assets en UI

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar la operacion backend canonica que permita `asset -> candidato 3D`
reutilizando la linea 3D vigente del repo y publicando outputs canonicos bajo
`Assets3D/`.

## Minimal Context
El repo ya tiene una ruta 3D activa validada, pero no una operacion de producto
encajada en el runner canonico para este paso concreto. Sin ella, la e2e UI de
phase `20` quedaria apoyada en tareas manuales o bridges ad hoc.

## Scope Budget (Roo/Qwen)
- Primary surface: `backend_contract`
- Max changed files: `6`
- Max code areas: `2 modulos` (`runner/backend contract` + `tests/docs canonicos`)
- Out-of-scope guardrail: no tocar rutas UI ni reabrir otras fases; no agregar runners paralelos.
- Split trigger: si cerrar la task exige >`6` archivos o >`2` areas, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/comfyui/3d-blender-bridge.md`
- `docs/architecture/runner-interface.md`
- `src/openclaw_studio/runners/comfyui.py`
- `docs/devplan/tasks/20.1-ui-asset-3d-import-or-modeling.md`

## Existing Infrastructure to Reuse
- Existing `Trellis2 GGUF` path from phase `11`
- Existing `Assets3D/` layout and 3D handoff docs
- Existing runner contract and evidence conventions
- Existing asset references from phase `19`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` and 3D provisioning paths already covered by the repo.

## Upstream Validation Gate
- Start only after Task `19.2` reports `pass` (hard gate).
- Reuse the validated asset references produced by phase `19`.
- The UI e2e for phase `20` must wait for this backend task to be complete.

## Do Not Create
- Do not create a second 3D runtime outside the current repo-supported path.
- Do not create a second evidence root outside `Assets3D/`.
- Do not bypass the canonical runner contract.

## Required Change
- Add a canonical backend operation for `asset -> 3D candidate`.
- Publish outputs, manifests, and readable status under canonical `Assets3D/`
  paths.
- Return enough metadata for later cleanup, rigging, and UI feedback.
- Keep the operation reusable by the UI phase without route-specific logic.

## Microtask Breakdown
- [ ] MT1: Acotar contrato backend y puntos de integracion exactos a tocar. files: `docs/devplan/tasks/20.0-backend-asset-3d-operation.md`, `src/openclaw_studio/runners/comfyui.py`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Implementar el slice minimo del backend sin duplicar runners/contratos. files: `src/openclaw_studio/runners/comfyui.py`, `src/openclaw_studio/implementations/builtin_flow_catalog.py`. verify: `PYTHONPATH=src python3 -m unittest tests -v`.
- [ ] MT3: Validar operacion canonica y cobertura de pruebas del flujo agregado. files: `src/openclaw_studio/implementations/builtin_flow_catalog.py`, `docs/architecture/runner-interface.md`. verify: `scripts/actions/runner-action.sh describe comfyui`.
- [ ] MT4: Actualizar docs canonicos y cerrar gate con trazabilidad revisable. files: `docs/devplan/tasks/20.0-backend-asset-3d-operation.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `src/openclaw_studio/runners/comfyui.py`
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/20.0-backend-asset-3d-operation.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests -v
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
scripts/actions/runner-action.sh list-targets comfyui operate
```

## Expected Evidence

* Expected output file: canonical 3D candidate outputs under `Assets3D/`
* Expected log/result file: backend run evidence and readable result payload
* Expected status: the repo can execute the `asset -> 3D` backend path without
  manual bridge work
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing 3D infrastructure is reused.
* No duplicate runtime or evidence root is introduced.
* The backend operation is consumable by the UI phase and later 3D phases.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
