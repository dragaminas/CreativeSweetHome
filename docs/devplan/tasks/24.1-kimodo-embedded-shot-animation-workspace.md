# Task 24.1: Workspace embebido de Kimodo para animacion de tomas

## Execution Header
You are working in the existing repository. Implement only Task 24.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate motion-authoring apps, forks, or proxy paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 24: Animacion de personajes en Kimodo embebido

## Status
`pending`

## Goal
Reutilizar la UI de `Kimodo` casi tal cual dentro del producto web mediante un
workspace embebido con contexto de escena, shot, personaje, estado y rutas de
salida.

## Minimal Context
`Kimodo` ya existe como backend local instalado por el repo. Esta fase no debe
reimplementar authoring de motion si la UI nativa ya cubre bien:

- timeline
- constraints
- authoring de movimiento

La app de producto debe darle marco y contexto, no sustituirla innecesariamente.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/12.1-kimodo-bootstrap-installation.md`
- `docs/kimodo/installation.md`
- `docs/devplan/tasks/23.1-ui-shot-description-workspace.md`
- `apps/openclaw-ui/`
- `scripts/apps/install-kimodo.sh`

## Existing Infrastructure to Reuse
- Existing `Kimodo` installation path from phase `12`
- Existing product shell from phase `15`
- Existing shot and asset context from phases `18` and `23`
- Existing workspace and output roots under `STUDIO_DIR`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Kimodo` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `23.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated shot brief produced by phase `23`.
- Complete Task `24.0` before phase `24` is considered ready for e2e proof.
## Do Not Create
- Do not fork `Kimodo` just to restyle the whole tool in the MVP.
- Do not create a second motion-authoring UI beside the upstream one.
- Do not expose `Kimodo` as an external-feeling site with no product context.

## Required Change
- Add a dedicated `Kimodo` workspace or panel inside the product shell.
- Proxy or embed `Kimodo` under the product domain or app surface so it does
  not feel external.
- Pass surrounding context: scene, shot, character, asset state, and output
  paths.
- Surface product-level checkpoints for base animation created, saved, or
  published back to the pipeline.

## Deliverables
- updates under `apps/openclaw-ui/`
- any proxy, session, or context-passing code needed for the embedded workspace
- documentation updates if the chosen embed seam changes the agreed plan

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/24.1-kimodo-embedded-shot-animation-workspace.md`
- `docs/SAD.md` only if the integration changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- kimodo-workspace
```

### Integration / smoke validation
```bash
bash scripts/apps/install-kimodo.sh audit
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase24-kimodo
```

## Expected Evidence

* Expected output file: product route or workspace for `Kimodo` plus saved
  animation context linked to the shot
* Expected log/result file: e2e evidence showing embedded launch and context
  handoff
* Expected status: `Kimodo` feels embedded inside the product shell and
  receives canonical context without a duplicate authoring UI
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `Kimodo` infrastructure is reused.
* No duplicate motion-authoring product path is introduced.
* The user receives a dedicated embedded workspace with canonical context.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
