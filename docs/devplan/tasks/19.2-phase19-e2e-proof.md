# Task 19.2: Prueba end-to-end de Phase 19

## Execution Header
You are working in the existing repository. Implement only Task 19.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical product flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 19: Referencias de assets con ComfyUI encapsulado

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real la generacion o importacion de referencias desde la
UI, con `ComfyUI` tratado solo como engine y con evidencia revisable.

## Minimal Context
La fase se cierra cuando una persona puede seleccionar un asset catalogado,
lanzar el paso de referencias desde la UI y obtener previews, artefactos y
evidencia final sin tocar la UI general de `ComfyUI`.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/19.1-ui-asset-reference-images.md`
- `docs/comfyui/interface.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/comfyui.py`

## Existing Infrastructure to Reuse
- Existing asset catalog flow from phase `18`
- Existing web shell from phase `15`
- Existing `ComfyUI` runtime and runner path from phase `8`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `18.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated asset catalog produced by phase `18`.
- Execute this proof only after the implementation tasks of phase `19` and Task `19.0` and the asset-reference UI route are complete.
## Do Not Create
- Do not replace the real proof with only unit tests or mocked adapters.
- Do not expose the raw `ComfyUI` UI as part of the proof.
- Do not publish evidence outside canonical asset-owned paths.

## Required Change
- Run a real browser-backed reference flow from the product UI.
- Exercise progress, preview, and final artifact handling.
- Publish the real outcome of the run and document any runtime blockers.

## Deliverables
- real reference-generation evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/19.2-phase19-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/19.2-phase19-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- asset-references
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase19-asset-references
```

## Expected Evidence

* Expected output file: imported or generated references stored in canonical
  asset-owned paths
* Expected log/result file: e2e report plus run evidence for previews and final
  artifacts
* Expected status: real `pass` for the product reference flow or explicit
  blocker with diagnosis
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or product path is introduced.
* A real reference flow completes through the product UI with `ComfyUI`
  remaining hidden as an engine.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
