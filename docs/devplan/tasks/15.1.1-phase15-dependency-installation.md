# Task 15.1.1: Instalacion de dependencias de Phase 15

## Execution Header
You are working in the existing repository. Implement only Task 15.1.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate installers, config sources, services, or web build
  paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 15: Producto UI web con SvelteKit y workspaces embebidos

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar la ruta canonica de dependencias para la UI web del producto de modo
que `SvelteKit`, `npm` y la validacion e2e del shell no dependan de pasos
manuales fuera del repo.

## Minimal Context
El repo ya instala `OpenClaw` con `node` y `npm`, pero todavia no existe una
ruta canonica para:

- dependencias del frontend `SvelteKit`
- instalacion o auditoria de navegadores para `Playwright`
- visibilidad de esta capacidad desde `.env`, bootstrap y `show-config`

La fase `15` no debe dejar este camino como prose narrativa.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/tasks/15.1-sveltekit-product-shell-and-embedded-workspaces.md`
- `.env.example`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `scripts/lib/common.sh`
- `scripts/openclaw/install-base-deps.sh`
- `scripts/openclaw/install-openclaw.sh`
- `docs/operations/bootstrap.md`

## Existing Infrastructure to Reuse
- Existing bootstrap entrypoint: `scripts/bootstrap/apply-workstation.sh`
- Existing config source and defaults: `.env.example`, `scripts/lib/common.sh`
- Existing config visibility: `scripts/bootstrap/show-config.sh`
- Existing `node` and `npm` provisioning: `scripts/openclaw/install-base-deps.sh`
- Existing repo convention for app installers: `scripts/apps/`

## Dependency Provisioning
- This task must deliver a new canonical script:
  `scripts/apps/install-ui-web-deps.sh`
- That script must be hooked into the bootstrap through `.env`
- The script must cover `audit` and `apply`
- The route must reuse existing repo-wide `node` and `npm` provisioning instead
  of inventing a second runtime installer

## Upstream Validation Gate
- Phase `15` starts the UI pipeline and has no prior UI gate.
- Reuse only canonical repo invariants, the runner contract, and validated foundations from phases `0-14`.
- Do not mark phase `15` ready until this dependency route and Task `15.1` are complete.
## Do Not Create
- Do not create a second bootstrap outside `scripts/bootstrap/apply-workstation.sh`.
- Do not create a second config source outside `.env`.
- Do not require manual `npm install` or browser setup as the only documented path.
- Do not create a second e2e dependency installer outside the canonical script.

## Required Change
- Create `scripts/apps/install-ui-web-deps.sh` as the canonical dependency
  script for the product web UI.
- Add declarative flags in `.env.example` for enabling UI dependency
  provisioning and the chosen app directory.
- Hook the script into `scripts/bootstrap/apply-workstation.sh` and
  `scripts/bootstrap/show-config.sh`.
- Audit at least: `node`, `npm`, the app directory, lockfile state, and
  `Playwright` browser availability.
- Support `apply` mode to install app dependencies and e2e browser assets
  through the repo-supported path.

## Deliverables
- `scripts/apps/install-ui-web-deps.sh`
- `.env.example`
- `scripts/lib/common.sh`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `docs/operations/bootstrap.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/15.1.1-phase15-dependency-installation.md`
- `docs/operations/bootstrap.md`
- `docs/SAD.md` only if this dependency path changes current supported
  architecture

## Verification

### Static checks
```bash
bash -n scripts/apps/install-ui-web-deps.sh
bash -n scripts/bootstrap/apply-workstation.sh
bash -n scripts/bootstrap/show-config.sh
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
bash scripts/apps/install-ui-web-deps.sh audit
OPENCLAW_UI_INSTALL=true scripts/bootstrap/show-config.sh
OPENCLAW_UI_INSTALL=true scripts/bootstrap/apply-workstation.sh audit
```

## Expected Evidence

* Expected output file: `scripts/apps/install-ui-web-deps.sh`
* Expected log/result file: audit or apply output for the web dependency path
  and readable status from bootstrap
* Expected status: the repo has a canonical, reproducible dependency route for
  the `SvelteKit` product shell
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing bootstrap and config infrastructure is reused.
* No duplicate installer or config source is introduced.
* Web dependency provisioning is available through the repo-supported path.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
