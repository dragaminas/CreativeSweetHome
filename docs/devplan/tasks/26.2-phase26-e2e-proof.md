# Task 26.2: Prueba end-to-end de Phase 26

## Execution Header
You are working in the existing repository. Implement only Task 26.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical shot-composition flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 26: Composicion automatizada de toma en Blender

## Status
`pending`

## Goal
Probar en una corrida real la composicion automatizada de una toma desde la UI
hasta la publicacion de outputs revisables en `Exports/`.

## Minimal Context
La fase se cierra cuando una toma descrita y con assets listos puede completar
la composicion automatizada desde el producto web con evidencia real.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/26.1-ui-blender-shot-composition.md`
- `docs/comfyui/blender-bridge.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing shot description and animation flows from phases `23-25`
- Existing `Exports/` layout and `Blender` integration
- Existing product shell from phase `15`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Blender` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `25.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated applied-animation state produced by phase `25`.
- Execute this proof only after the implementation tasks of phase `26` and Task `26.0` and the shot-composition UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not publish composition evidence outside canonical export paths.

## Required Change
- Run a real browser-backed shot-composition flow.
- Verify resulting outputs and manifests under canonical `Exports/` paths.
- Publish the real outcome of the proof.

## Deliverables
- real shot-composition proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/26.2-phase26-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/26.2-phase26-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- shot-composition
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe blender
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase26-shot-composition
```

## Expected Evidence

* Expected output file: composed shot outputs under canonical `Exports/` paths
* Expected log/result file: e2e report plus artifact refs and composition diagnostics
* Expected status: real `pass` for shot composition or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or export path is introduced.
* A real shot-composition run is exercised through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
