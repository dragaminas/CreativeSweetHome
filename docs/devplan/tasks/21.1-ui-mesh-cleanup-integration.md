# Task 21.1: Integracion UI del cleanup de meshes

## Execution Header
You are working in the existing repository. Implement only Task 21.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate cleanup pipelines, CLIs, or evidence roots.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 21: Cleanup automatizado de meshes en UI

## Status
`pending`

## Goal
Integrar en la UI principal la fase `13` de cleanup pre-rigging como una
accion guiada, visible y legible para personas no tecnicas.

## Minimal Context
La infraestructura real de cleanup ya existe en la fase `13`. Esta tarea no
debe reimplementar el backend; debe consumirlo desde la UI usando el contrato
canonico y la evidencia ya establecida.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/13.1-blender-instant-meshes-pre-rigging-cleanup.md`
- `docs/operations/3d-pre-rig-cleanup.md`
- `docs/architecture/runner-interface.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/blender.py`

## Existing Infrastructure to Reuse
- Existing `blender` runner from phase `13`
- Existing `Assets3D/<project>/<entity_id>/cleanup/<run_id>/` evidence layout
- Existing product shell from phase `15`
- Existing asset 3D flow from phase `20`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the phase `13` dependency path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `20.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated 3D candidate produced by phase `20`.
- Reuse the canonical cleanup handoff from phase `13` so Task `21.2` validates the same output roots.
## Do Not Create
- Do not create a second cleanup backend or wrapper only for the UI.
- Do not create a second evidence tree outside the canonical cleanup layout.
- Do not replace runner statuses with a UI-only taxonomy.

## Required Change
- Add a cleanup workspace or panel for 3D assets ready for pre-rigging.
- Invoke the existing runner path instead of a new CLI.
- Surface progress, cancellation, before/after evidence, cleanup report, and
  beginner-readable diagnostics.
- Mark when a model is ready for rigging or still blocked.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `docs/devplan/UIPlan.md` if integration decisions change the agreed plan

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/21.1-ui-mesh-cleanup-integration.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
python3 -m compileall src tests
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- mesh-cleanup
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe blender
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase21-mesh-cleanup
```

## Expected Evidence

* Expected output file: UI-linked cleanup artifacts under canonical cleanup paths
* Expected log/result file: e2e or adapter evidence showing progress,
  before/after refs, and cleanup report access
* Expected status: the UI can consume the existing cleanup phase without a
  duplicate pipeline
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing cleanup runner and evidence layout are reused.
* No duplicate cleanup backend or path is introduced.
* The UI exposes readable progress, evidence, and readiness feedback.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
