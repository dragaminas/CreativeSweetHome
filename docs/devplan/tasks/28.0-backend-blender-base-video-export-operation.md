# Task 28.0: Backend canonico de export base de video en Blender

## Execution Header
You are working in the existing repository. Implement only Task 28.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate export-video pipelines, export roots, or state models.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 28: Exportacion de videos base de tomas

## Status
`pending`

## Goal
Cerrar la operacion backend canonica que exporta el video base de una toma
desde `Blender` y lo deja listo para las fases AI posteriores.

## Minimal Context
La fase `28` ya tiene una task de dependencias, pero falta la operacion backend
real de export. Sin ella, la UI `28` no tendria un backend ejecutable que
probar end-to-end.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/comfyui/blender-bridge.md`
- `docs/operations/blender.md`
- `docs/devplan/tasks/27.0-backend-blender-refine-session-bridge.md`
- `docs/devplan/tasks/28.1-ui-base-video-export.md`

## Existing Infrastructure to Reuse
- Existing refine-state output from phase `27`
- Existing `Exports/` layout and `Blender -> ComfyUI` bridge
- Existing dependency path from Task `28.1.1`
- Existing product shell conventions for evidence and state

## Planned Dependency Path
- Reuse the dedicated dependency task
  `docs/devplan/tasks/28.1.1-phase28-dependency-installation.md`
- Reuse the canonical script
  `scripts/apps/install-blender-video-export-deps.sh`
- Do not create a second host-readiness path for video export

## Upstream Validation Gate
- Start only after Task `27.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated refined shot context from phase `27`.
- The UI e2e for phase `28` must wait for this backend task to be complete.

## Do Not Create
- Do not create a second base-video export pipeline outside the canonical path.
- Do not create a new output root outside `Exports/`.
- Do not leave the export operation implicit behind only dependency checks.

## Required Change
- Implement a canonical backend operation for base-video export in `Blender`.
- Publish video base, key frames, manifests, and diagnostics under canonical
  shot export paths.
- Return a canonical status payload reusable by phases `29` and `30`.

## Deliverables
- backend export operation code and any runner integration needed
- canonical output or manifest adapter for base-video export
- minimal docs updates describing the operation contract

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/28.0-backend-blender-base-video-export-operation.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests -v
```

### Integration / smoke validation
```bash
bash scripts/apps/install-blender-video-export-deps.sh audit
```

## Expected Evidence

* Expected output file: canonical base-video outputs under `Exports/`
* Expected log/result file: backend export evidence and readable result payload
* Expected status: the repo can export a base shot video through a canonical backend path
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `Blender`, export, and dependency infrastructure is reused.
* No duplicate export pipeline or output root is introduced.
* The backend operation is sufficient for the UI phase and later `ComfyUI` phases.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
