# Task 14.1.2: Target canonico `create_rig_humanoid` en runner Blender

## Execution Header
You are working in the existing repository. Implement only Task 14.1.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 14: Rigging humanoide automatizado con Blender y Rigify

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Implementar en el runner canonico `blender` el target `create_rig_humanoid`
con evidencia automatica minima de poses/deformaciones para desbloquear el gate
tecnico que hoy mantiene la Task `14.2` en `blocked`.

## Minimal Context
La fase `14` ya tiene planning (`14.1`) y provisioning (`14.1.1`) cerrados. El
bloqueo real confirmado en `14.2` es estrictamente de backend: el runner
`blender` no reconoce `target_id=create_rig_humanoid` y por eso la prueba e2e
falla en `fail_compile` antes de correr el flujo de rigging.

Esta tarea debe cerrar ese gap sin abrir superficies nuevas de UI ni pipelines
paralelos: solo contrato backend canonico, helper de Blender en background,
status canonicos y evidencia revisable bajo `Assets3D`.

## Scope Budget (Roo/Qwen)
- Primary surface: `backend_contract`
- Target changed files (soft cap): `6`
- Hard cap (must stop and split): `8`
- Max code areas: `2 modulos` (`runner blender` + `helper/script de rigging y tests`)
- Out-of-scope guardrail: no cambios en UI web, sin refactor de arquitectura, sin nuevos runners o CLIs paralelas.
- Split trigger: si cerrar la task supera el soft cap (`6`) o expande areas no previstas, detener implementacion, abrir task hermana y replanificar antes de seguir.
- Overflow protocol: al alcanzar hard cap (`8`), detener implementacion, marcar status `blocked_scope_split_required`, registrar gap tecnico y enlazar task hermana en `docs/devplan/01-phase-index.md`.

## Files to Inspect First
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/14.1-blender-rigify-humanoid-rigging-plan.md`
- `docs/devplan/tasks/14.1.1-phase14-dependency-installation.md`
- `docs/devplan/tasks/14.2-phase14-e2e-proof.md`
- `docs/architecture/runner-interface.md`
- `scripts/actions/runner-action.sh`
- `scripts/apps/blender.sh`
- `scripts/apps/blender_rigging_smoke_test.py`
- `src/openclaw_studio/runners/blender.py`
- `tests/test_blender_runner.py`
- `docs/operations/blender.md`

## Existing Infrastructure to Reuse
- Existing runner contract and statuses: `docs/architecture/runner-interface.md`
- Existing runner bridge: `scripts/actions/runner-action.sh`
- Existing `blender` runner and evidence pattern from phase `13`
- Existing dependency path from task `14.1.1`: `scripts/apps/install-3d-rigging-deps.sh`
- Existing canonical handoff from cleanup: `Assets3D/.../cleanup/<run_id>/output/`

## Dependency Provisioning
- No new host dependencies are introduced in this task.
- Reuse `scripts/apps/install-3d-rigging-deps.sh` (`audit`/`apply`) from Task
  `14.1.1`.
- Reuse bundled `Rigify` path in Blender; do not add paid or web-only tooling.

## Upstream Validation Gate
- Start only after Task `13.3` reports `pass` or `soft_pass_with_fallback`
  (hard gate for prepared humanoid handoff).
- Start only after Task `14.1.1` reports `done` (dependency gate).
- Task `14.2` can be re-run only after this task exposes
  `create_rig_humanoid` and publishes pose/deformation evidence canonica.

## Source of Truth Matrix
- Domain: `task_status`.
- Authoritative Source: `docs/devplan/tasks/14.1.2-phase14-create-rig-runner-target.md` (`## Status` y `Implementation Outcome` cuando aplique).
- Derived/Projection Artifacts: `docs/devplan/task-status-index.md`, `docs/devplan/01-phase-index.md`.
- Reconciliation Command: `python3 scripts/devplan/build-task-status-index.py --write`.
- Write Rule: editar primero esta task file y luego regenerar el indice de estado.
- Domain: `runtime_state`.
- Authoritative Source: `src/openclaw_studio/runners/blender.py`, `scripts/apps/blender.sh`, helper de rigging y evidencia en `STUDIO_DIR/Assets3D/.../rigging/<run_id>/`.
- Derived/Projection Artifacts: resumenes de fase/feature (`docs/devplan/01-phase-index.md`, `docs/devplan/feature-map.md`).
- Reconciliation Command: `scripts/actions/runner-action.sh list-targets blender operate`.
- Write Rule: actualizar primero artefacto operativo canonico; luego sincronizar documentacion derivada.
- Domain: `validation_evidence`.
- Authoritative Source: outputs y manifests de `Verification`.
- Derived/Projection Artifacts: notas de cierre en task docs.
- Reconciliation Command: ejecutar todos los comandos de `Verification` de esta task sin atajos.
- Write Rule: no marcar `done` si no existe evidencia trazable del target y de la pose-suite automatica.

## Implementation Contract (No-Drift)
- Primary authority boundary: operacion backend canonica `blender/operate/create_rig_humanoid` y evidencia automatica de validacion.
- Explicit non-authority boundary: no UI routes, no taxonomias de estado paralelas, no nuevo runner, no nueva CLI de orquestacion.
- Required inputs (existing artifacts): modelo `cleaned/remeshed` valido de fase `13`, `project_id`, `entity_id`, y wrappers/scripts canonicos existentes.
- Required outputs (exact paths): evidencia bajo `STUDIO_DIR/Assets3D/<project>/<entity_id>/rigging/<run_id>/` con manifests, logs, rig exportado y `rigging-report`.
- Sequencing constraints: registrar target en runner -> validar request/input contract -> ejecutar helper Blender background -> publicar evidencia canonica -> cubrir con tests -> actualizar docs operativas.
- Failure policy: reportar `blocked_*`/`fail_*` con causa tecnica concreta y ruta afectada; nunca inventar rutas o estados paralelos.
- Evidence policy: cada claim de avance debe mapear a archivo/log revisable en repo o `STUDIO_DIR`.

## Detailed Execution Context (Anti-Invention)
- Canonical authority model: runner `blender` es la unica entrada de orquestacion para este paso.
- Mandatory guardrails: reutilizar infraestructura existente, no duplicar contratos, no mover evidencia fuera del layout canonico.
- Gate discipline: si falla cualquier prerequisito upstream, mantener `blocked_*` explicito.
- Evidence discipline: toda validacion de poses/deformaciones debe quedar en reporte revisable.
- Drift control: si para cerrar esta task hay que tocar UI o mas de `2` areas de codigo, detener y abrir task hermana.
- Roo/Codex handoff note: ejecutar microtasks en orden; cualquier desviacion debe justificarse y reflejarse en esta task file.
- Context note: esta task no reemplaza la prueba e2e de fase; solo habilita su ejecucion real.

## Do Not Create
- Do not create a second rigging orchestration path outside `scripts/actions/runner-action.sh` + `runner_cli`.
- Do not create a second evidence root outside `Assets3D/.../rigging/<run_id>/`.
- Do not replace the pose/deformation validation with mocks only.
- Do not require Blender UI manual editing in the happy path.

## Required Change
- Extender el runner `blender` para exponer `target_id=create_rig_humanoid`
  bajo `operation_kind=operate`.
- Implementar la ejecucion backend en Blender background con `Rigify` +
  auto-weights y pose-suite minima (`arms`, `elbows`, `knees`, `head`, `torso`).
- Publicar evidencia canonica: manifests, logs, export riggeado y
  `rigging-report` legible para principiantes.
- Emitir statuses canonicos (`pass`, `soft_pass_with_fallback`, `fail_quality`,
  `blocked_*`, `fail_runtime`) sin taxonomia nueva.
- Mantener compatibilidad directa con el gate de Task `14.2`.

## Microtask Breakdown
- [x] MT1: Delimitar contrato final del target y estados canonicos de salida. files: `docs/devplan/tasks/14.1.2-phase14-create-rig-runner-target.md`, `src/openclaw_studio/runners/blender.py`. verify: `scripts/devplan/check-task-files.sh`.
- [x] MT2: Implementar registro del target y flujo minimo de request/evidence en runner sin duplicar contratos. files: `src/openclaw_studio/runners/blender.py`, `tests/test_blender_runner.py`. verify: `PYTHONPATH=src python3 -m unittest tests.test_blender_runner -v`.
- [x] MT3: Implementar helper de Blender background para `create_rig_humanoid` con pose-suite y reporte de deformaciones. files: `scripts/apps/blender.sh`, `scripts/apps/blender_create_rig_humanoid.py`, `tests/test_blender_runner.py`. verify: `bash -n scripts/apps/blender.sh && python3 -m compileall scripts/apps`.
- [x] MT4: Sincronizar documentacion operativa y dejar comando de gate listo para reintentar `14.2`. files: `docs/operations/blender.md`, `docs/devplan/tasks/14.1.2-phase14-create-rig-runner-target.md`. verify: `scripts/actions/runner-action.sh list-targets blender operate`.

## Deliverables
- `src/openclaw_studio/runners/blender.py`
- `scripts/apps/blender.sh`
- `scripts/apps/blender_create_rig_humanoid.py`
- `tests/test_blender_runner.py`
- `docs/operations/blender.md`
- `docs/devplan/tasks/14.1.2-phase14-create-rig-runner-target.md`

## Canonical Docs to Update
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/task-status-index.md`
- `docs/devplan/tasks/14.1.2-phase14-create-rig-runner-target.md`
- `docs/operations/blender.md`
- `docs/devplan/tasks/14.2-phase14-e2e-proof.md` only if the gate wording must be updated after implementation

## Verification

### Static checks
```bash
bash -n scripts/apps/blender.sh
python3 -m compileall src tests scripts/apps
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests.test_blender_runner -v
```

### Integration / smoke validation
```bash
bash scripts/apps/install-3d-rigging-deps.sh audit
scripts/actions/runner-action.sh describe blender
scripts/actions/runner-action.sh list-targets blender operate
scripts/actions/runner-action.sh start blender operate create_rig_humanoid
```

## Expected Evidence

* Expected output file: soporte real del target `create_rig_humanoid` en el runner `blender`
* Expected log/result file: evidencia canonica bajo `Assets3D/.../rigging/<run_id>/` con manifests/logs/report, o `blocked_missing_asset` aceptado cuando no se provean inputs
* Expected status: el target deja de devolver `fail_compile` por desconocido y publica estado canonico
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* The canonical target `create_rig_humanoid` is exposed in the `blender` runner.
* Pose/deformation evidence is automatically published in canonical paths.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.

## Implementation Outcome (2026-05-17)

* implementation status: `done`
* runner contract outcome:
  - `blender/operate` ahora expone `create_rig_humanoid` junto a `cleanup_pre_rig_humanoid`.
  - el target deja de responder `fail_compile` por desconocido y publica estados canonicos.
* TDD evidence:
  - RED: `PYTHONPATH=src python3 -m unittest tests.test_blender_runner -v` fallo al importar `TARGET_ID_CREATE_RIG_HUMANOID` inexistente en el runner.
  - GREEN: tras implementar runner + helper, la misma suite queda en `8` tests `OK`.
* integration evidence:
  - `bash scripts/apps/install-3d-rigging-deps.sh audit` -> `status=pass` (Rigify/export/render disponibles en background).
  - `scripts/actions/runner-action.sh describe blender` -> `display_label=\"Blender + Instant Meshes + Rigify\"`.
  - `scripts/actions/runner-action.sh list-targets blender operate` -> incluye `create_rig_humanoid` con `required_inputs=[\"prepared_model_path\"]` y pose-suite `basic_humanoid_v1`.
  - `scripts/actions/runner-action.sh start blender operate create_rig_humanoid` (sin inputs) -> `accepted=true`, `status=blocked_missing_asset`, evidencia publicada bajo `Assets3D/.../rigging/rigging-20260517-154454/`.
  - corrida real de smoke con input `.obj` minimo -> `status=pass`, `run_id=rigging-20260517-154602`, evidencia completa en `Assets3D/phase14/dummy-humanoid/rigging/rigging-20260517-154602/` (manifests, logs, glb/fbx, previews por pose, `rigging-report.md`).
* gate impact:
  - se cierra el bloqueo tecnico backend que mantenia `14.2` en `fail_compile` por target inexistente.
