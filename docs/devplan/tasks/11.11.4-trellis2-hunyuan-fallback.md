# Task 11.11.4: Fallback automatico de Trellis2 a Hunyuan3D

## Execution Header
You are working in the existing repository. Implement only Task 11.11.4.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 11: Reapertura 3D en ComfyUI con Trellis2 GGUF

## Status
`archived`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Registrar que el fallback automatico a `Hunyuan3D` queda fuera del alcance
activo actual del repo.

## Minimal Context
La lectura actual del repo ya no trata `Hunyuan3D` como ruta estable activa.
Por eso este task deja de ser ejecutable y pasa a registro historico de una
opcion descartada.

Debe seguir siendo compatible:

- la coherencia del `DevPlan`
- la lectura actual de `docs/SAD.md`

Fuera de alcance:

- implementar un fallback a `Hunyuan3D`

## Files to Inspect First
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/SAD.md`

## Existing Infrastructure to Reuse
- Existing scope docs: `docs/SAD.md`, `docs/devplan/01-phase-index.md`
- Existing Trellis interface: `docs/comfyui/trellis2-gguf-interface.md`

## Do Not Create
- Do not create a new fallback plan to `Hunyuan3D`.
- Do not reopen this track through ad hoc notes outside the task file.

## Required Change
- Marcar el task como fuera de alcance.
- Sincronizar la lectura de la fase con la salida de `Hunyuan3D` del alcance
  activo actual.

## Deliverables
- `docs/devplan/tasks/11.11.4-trellis2-hunyuan-fallback.md`

## Canonical Docs to Update
- `docs/SAD.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/11.11.4-trellis2-hunyuan-fallback.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
true
```

### Integration / smoke validation
```bash
scripts/devplan/check-task-files.sh
```

## Expected Evidence

* Expected output file: `docs/devplan/tasks/11.11.4-trellis2-hunyuan-fallback.md`
* Expected log/result file: task archivado y lectura de alcance sincronizada
* Expected status: fallback a `Hunyuan3D` fuera del alcance activo
* Expected exit code: `0` para el check mecanico de task files

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
