# Task 11.11.4: Fallback automatico de Trellis2 a Hunyuan3D

## Execution Header
You are working in the existing repository. Implement only Task 11.11.4.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 11: Reapertura 3D en ComfyUI con Trellis2 GGUF

## Status
`pending`

## Goal
Implementar el fallback automatico a `Hunyuan3D` cuando falle el preflight de
Trellis2 o falten dependencias criticas.

## Minimal Context
`Hunyuan3D` es la ruta estable actual y Trellis2 sigue condicionado por su
preflight. El fallback debe apoyarse en esa realidad y no crear un segundo
canal de decision.

Debe seguir siendo compatible:

- el registro de runners existente
- la seleccion de variantes del catalogo
- la documentacion de fallbacks de `Hunyuan3D`

Fuera de alcance:

- fallback a `SF3D`
- un selector manual aislado solo para Trellis2

## Files to Inspect First
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/hunyuan3d/fallback-paths.md`
- `scripts/apps/install-trellis2-gguf.sh`
- `scripts/apps/comfyui-trellis2-gguf-validation.sh`

## Existing Infrastructure to Reuse
- Existing stable fallback path: `docs/hunyuan3d/fallback-paths.md`
- Existing flow catalog: `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- Existing selection logic: `src/openclaw_studio/application/session_engine.py`
- Existing preflight signal: `pass_preflight` vs `blocked_*`

## Do Not Create
- Do not create a parallel fallback catalog.
- Do not create a Trellis-only runner when the existing runners already exist.
- Do not create a second fallback document outside the cited docs.

## Required Change
- Hacer que la seleccion o ejecucion degrade automaticamente a `Hunyuan3D`
  cuando Trellis2 no pase preflight o no tenga dependencias criticas.
- Mantener el mismo alias de producto y la misma ruta de entrada para la UI.
- Documentar el fallback y sus mensajes de operador.

## Deliverables
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/hunyuan3d/fallback-paths.md`

## Canonical Docs to Update
- `docs/SAD.md` si cambia la lectura actual del fallback 3D
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/11.11.4-trellis2-hunyuan-fallback.md`
- `docs/devplan/archive/phase-10-hunyuan3d-summary.md` si el fallback redefine la lectura del baseline vigente

## Verification

### Static checks
```bash
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests.test_flow_catalog -v
```

### Integration / smoke validation
```bash
bash scripts/apps/install-trellis2-gguf.sh || true
PYTHONPATH=src python3 -m openclaw_studio
scripts/devplan/check-task-files.sh
```

## Expected Evidence

* Expected output file: `docs/comfyui/trellis2-gguf-interface.md`
* Expected log/result file: criterio de fallback automatico y mensajes legibles al operador
* Expected status: Trellis2 cae a `Hunyuan3D` sin cambiar alias ni romper la operacion
* Expected exit code: comportamiento verificable ante `blocked_*`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
