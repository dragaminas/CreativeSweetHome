# Task 17.1: Scaffolding UI de almacenamiento de escena

## Execution Header
You are working in the existing repository. Implement only Task 17.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate roots, scene layouts, or manifest families.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 17: Estructura automatizada de almacenamiento de escena

## Status
`pending`

## Goal
Implementar desde la UI la creacion automatizada de la estructura de
almacenamiento de una escena ampliando `STUDIO_DIR` y los layouts ya canonicos
del repo.

## Minimal Context
El repo ya usa filesystem-first para `Assets3D/` y `Exports/`. Esta fase debe
evitar una segunda raiz de trabajo y dejar una estructura clara de escena,
assets y shots que sirva a los pasos posteriores.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/16.1-ui-scene-description-workspace.md`
- `scripts/openclaw/setup-workspace.sh`
- `docs/comfyui/blender-bridge.md`
- `docs/comfyui/3d-blender-bridge.md`
- `.env.example`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing workspace root: `STUDIO_DIR`
- Existing layout roots: `Assets3D/`, `Exports/`
- Existing setup script: `scripts/openclaw/setup-workspace.sh`
- Existing shell and manifest handling from phases `15` and `16`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing filesystem and bootstrap path rooted in `STUDIO_DIR`.

## Upstream Validation Gate
- Start only after Task `16.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated scene brief produced by phase `16`.
- Keep the scaffold rooted in canonical scene storage paths so Task `17.2` validates the same structure.
## Do Not Create
- Do not create a second workspace root outside `STUDIO_DIR`.
- Do not introduce a new database-only scene source of truth for the MVP.
- Do not create a scene layout that diverges from existing `Assets3D/` and
  `Exports/` conventions without a documented reason.

## Required Change
- Define the canonical scene scaffold created by the product UI.
- Generate the directory tree and initial manifests from a saved scene brief.
- Reuse existing repo layout conventions wherever possible.
- Surface collisions, missing prerequisites, and created paths as readable UI
  feedback.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `scripts/openclaw/setup-workspace.sh`
- `docs/devplan/UIPlan.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/17.1-ui-scene-storage-scaffolding.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- scene-storage
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase17-scaffold
```

## Expected Evidence

* Expected output file: created scene scaffold under `STUDIO_DIR`
* Expected log/result file: e2e or route evidence showing created directories
  and manifests
* Expected status: the UI can create a reusable scene storage scaffold
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing filesystem roots and layout conventions are reused.
* No duplicate workspace or manifest family is introduced.
* The scaffold is created from the product UI with readable feedback.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
