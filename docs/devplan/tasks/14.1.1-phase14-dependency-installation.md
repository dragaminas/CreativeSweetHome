# Task 14.1.1: Instalacion de dependencias de Phase 14

## Execution Header
You are working in the existing repository. Implement only Task 14.1.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 14: Rigging humanoide automatizado con Blender y Rigify

## Status
`done`

## Goal
Cerrar la ruta canonica de instalacion o audit de dependencias para la fase
`14`, de modo que el flujo `prepared humanoid -> Rigify rigging -> validation`
no dependa de activaciones manuales ocultas ni de pasos fuera del repo.

## Minimal Context
La task `14.1` ya dejo fijado que el baseline `v1` de la fase usa:

- `Blender`
- `Rigify` bundled
- `Armature Deform With Automatic Weights`
- export local `glTF/FBX` y previews de validacion

Aunque no haya un binario externo tan evidente como `Instant Meshes`, la fase
si abre una nueva superficie de provision operativa:

- validar una version de `Blender` soportada para el flujo de rigging
- auditar disponibilidad real de `Rigify`
- asegurar activacion reproducible del add-on en background
- comprobar export y render basicos para evidencia

La metodologia del repo ya exige que esto no quede solo como prose en una task
de planning. Hace falta una tarea hoja dedicada para convertir esa ruta en
script canonico del repo antes del gate e2e de la fase.

## Files to Inspect First
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/14.1-blender-rigify-humanoid-rigging-plan.md`
- `scripts/lib/common.sh`
- `.env.example`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `scripts/apps/install-3d-pre-rig-deps.sh`
- `scripts/apps/blender.sh`
- `docs/operations/bootstrap.md`
- `docs/operations/blender.md`

## Existing Infrastructure to Reuse
- Existing bootstrap entrypoint: `scripts/bootstrap/apply-workstation.sh`
- Existing config source and defaults: `.env.example`, `scripts/lib/common.sh`
- Existing config visibility: `scripts/bootstrap/show-config.sh`
- Existing installer pattern from phase `13`: `scripts/apps/install-3d-pre-rig-deps.sh`
- Existing Blender wrapper and smoke scripts: `scripts/apps/blender.sh`, `scripts/apps/blender_smoke_test.py`

## Dependency Provisioning
- Esta tarea debe entregar un script canonico nuevo:
  `scripts/apps/install-3d-rigging-deps.sh`
- Ese script debe engancharse al bootstrap central por flags de `.env`
- Si durante la implementacion se concluye que no hay dependencias nuevas mas
  alla de `Blender` ya provisionado, el script igualmente debe existir como
  ruta de `audit` para `Rigify`, export y render; no basta con dejarlo como
  nota narrativa

## Do Not Create
- Do not create a second workstation bootstrap outside `scripts/bootstrap/apply-workstation.sh`.
- Do not create a second config source outside `.env`.
- Do not replace the planned rigging runner with installer-specific CLIs.
- Do not assume that `Rigify` being bundled means there is no provisioning or audit work to automate.

## Required Change
- Crear `scripts/apps/install-3d-rigging-deps.sh` como instalador o auditor
  canonico e idempotente para la fase `14`.
- Integrarlo en `scripts/bootstrap/apply-workstation.sh` mediante flags
  declarativas de `.env` y exponerlas tambien en
  `scripts/bootstrap/show-config.sh`.
- Validar y documentar como se audita `Rigify` en ejecucion background, y como
  se fuerza su activacion reproducible si la version de `Blender` lo requiere.
- Publicar una smoke validation barata para el baseline de la fase:
  disponibilidad de `Blender`, `Rigify`, export y preview/render.

## Deliverables
- `scripts/apps/install-3d-rigging-deps.sh`
- `scripts/apps/blender.sh`
- `scripts/apps/blender_rigging_smoke_test.py`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `scripts/lib/common.sh`
- `.env.example`
- `docs/operations/bootstrap.md`
- `docs/operations/blender.md`

## Canonical Docs to Update
- `docs/SAD.md` solo si la provision de fase `14` cambia la lectura de la
  arquitectura actual soportada
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/14.1.1-phase14-dependency-installation.md`
- `docs/operations/bootstrap.md`
- `docs/operations/blender.md`

## Verification

### Static checks
```bash
bash -n scripts/apps/install-3d-rigging-deps.sh
bash -n scripts/bootstrap/apply-workstation.sh
bash -n scripts/bootstrap/show-config.sh
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
bash scripts/apps/install-3d-rigging-deps.sh audit
RIGGING_3D_DEPS_INSTALL=true scripts/bootstrap/show-config.sh
RIGGING_3D_DEPS_INSTALL=true scripts/bootstrap/apply-workstation.sh audit
```

## Expected Evidence

* Expected output file: `scripts/apps/install-3d-rigging-deps.sh`
* Expected log/result file: salida revisable de `install-3d-rigging-deps.sh audit`, `rigging-smoke-report.json` y del bootstrap en modo `audit`
* Expected status: la fase `14` tiene una ruta canonica de provision o audit de dependencias en el repo, aunque no anada binarios grandes nuevos
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* The dependency path for phase `14` is implemented as a repo script plus bootstrap hook, not as a manual checklist.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
