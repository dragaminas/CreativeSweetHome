# Task 25.2: Prueba end-to-end de Phase 25

## Execution Header
You are working in the existing repository. Implement only Task 25.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical animation-apply flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 25: Aplicacion automatizada de animacion a personajes

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real la aplicacion automatizada de animacion desde la UI
hasta la publicacion de salidas y evidencia revisables.

## Minimal Context
La fase se cierra cuando una animacion authored en `Kimodo` puede aplicarse a
un personaje ya riggeado por la ruta de producto y devolver evidencia real.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/25.1-ui-animation-application-to-characters.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing rigging handoff from phase `22`
- Existing `Kimodo` handoff from phase `24`
- Existing product shell from phase `15`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Kimodo` and `Blender` provisioning paths already covered
  by the repo.

## Upstream Validation Gate
- Start only after Task `24.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated embedded `Kimodo` animation state produced by phase `24`.
- Execute this proof only after the implementation tasks of phase `25` and Task `25.0` and the animation-application UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not publish evidence outside canonical repo-owned paths.

## Required Change
- Run a real browser-backed animation-application flow.
- Verify resulting state, reports, and output refs in canonical locations.
- Publish the real outcome of the proof.

## Deliverables
- real animation-application proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/25.2-phase25-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/25.2-phase25-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- animation-apply
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe blender
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase25-animation-apply
```

## Expected Evidence

* Expected output file: character animation outputs or state refs in canonical
  locations
* Expected log/result file: e2e report showing `Kimodo` to character handoff
  and published results
* Expected status: real `pass` for animation application or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or evidence path is introduced.
* A real animation-application run is exercised through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
