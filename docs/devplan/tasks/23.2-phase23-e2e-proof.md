# Task 23.2: Prueba end-to-end de Phase 23

## Execution Header
You are working in the existing repository. Implement only Task 23.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical shot workspace.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 23: Descripcion de tomas en UI

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real la creacion de una toma desde la UI hasta la
persistencia de un `shot brief` revisable.

## Minimal Context
La fase se cierra cuando una escena ya preparada puede producir, desde el
producto web, una toma vinculada a assets y lista para animacion o composicion.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/23.1-ui-shot-description-workspace.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing scene and asset flows from phases `16-18`
- Existing product shell from phase `15`
- Existing shot brief persistence path introduced by Task `23.1`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.

## Upstream Validation Gate
- Start only after Task `22.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated character and pipeline state produced by phase `22`.
- Execute this proof only after the implementation tasks of phase `23` and the shot-description workspace is complete.
## Do Not Create
- Do not replace the real proof with only unit tests.
- Do not keep the proof artifact only in volatile browser state.

## Required Change
- Run a real browser-backed shot-description flow.
- Persist the resulting shot brief in the canonical location.
- Publish the real outcome of the proof.

## Deliverables
- real shot-description e2e evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/23.2-phase23-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/23.2-phase23-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- shot-brief
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase23-shot-brief
```

## Expected Evidence

* Expected output file: persisted shot-brief artifact linked to an existing scene
* Expected log/result file: e2e report showing capture, validation, and save
* Expected status: real `pass` for shot-description capture or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or persistence path is introduced.
* A real shot brief is captured and persisted through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
