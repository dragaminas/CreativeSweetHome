# Task 29.2: Prueba end-to-end de Phase 29

## Execution Header
You are working in the existing repository. Implement only Task 29.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical initial-image flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 29: Imagen inicial desde video base y referencias

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real la generacion de imagen inicial desde la UI hasta la
publicacion de artefactos y evidencia revisables.

## Minimal Context
La fase se cierra cuando un shot con video base y referencias puede producir
desde la UI una imagen inicial real sin abrir la UI general de `ComfyUI`.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/29.1-ui-initial-image-from-base-video.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/comfyui.py`

## Existing Infrastructure to Reuse
- Existing base-video export flow from phase `28`
- Existing product shell from phase `15`
- Existing `ComfyUI` runtime and runner path from phase `8`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `28.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated base video produced by phase `28`.
- Execute this proof only after the implementation tasks of phase `29` and Task `29.0` and the initial-image UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not expose the raw `ComfyUI` UI as part of the proof.
- Do not publish evidence outside canonical repo-owned paths.

## Required Change
- Run a real browser-backed initial-image flow.
- Verify resulting images, previews, and run evidence under canonical paths.
- Publish the real outcome of the proof.

## Deliverables
- real initial-image proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/29.2-phase29-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/29.2-phase29-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- initial-image
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase29-initial-image
```

## Expected Evidence

* Expected output file: initial-image artifacts in canonical repo-owned paths
* Expected log/result file: e2e report plus previews and final image refs
* Expected status: real `pass` for initial-image generation or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or product path is introduced.
* A real initial-image flow completes through the product UI with `ComfyUI`
  hidden as an engine.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
