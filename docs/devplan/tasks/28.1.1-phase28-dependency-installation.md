# Task 28.1.1: Instalacion de dependencias de Phase 28

## Execution Header
You are working in the existing repository. Implement only Task 28.1.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate installers, config sources, or bootstrap paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 28: Exportacion de videos base de tomas

## Status
`pending`

## Goal
Cerrar la ruta canonica de auditoria o instalacion para el export de video base
desde `Blender`, incluyendo codecs, previews y prerequisitos tecnicos del host.

## Minimal Context
Aunque `Blender` ya exista como dependencia del repo, esta fase abre una
superficie operativa nueva:

- export de video reproducible
- auditoria de codecs o integracion `ffmpeg`
- verificacion de previews o frames exportados

Eso no debe quedar solo como checklist manual.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/tasks/28.1-ui-base-video-export.md`
- `.env.example`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `scripts/lib/common.sh`
- `scripts/apps/blender.sh`
- `docs/operations/blender.md`
- `docs/comfyui/blender-bridge.md`

## Existing Infrastructure to Reuse
- Existing bootstrap entrypoint: `scripts/bootstrap/apply-workstation.sh`
- Existing config source and defaults: `.env.example`, `scripts/lib/common.sh`
- Existing config visibility: `scripts/bootstrap/show-config.sh`
- Existing `Blender` wrapper: `scripts/apps/blender.sh`
- Existing repo convention for app installers: `scripts/apps/`

## Dependency Provisioning
- This task must deliver a new canonical script:
  `scripts/apps/install-blender-video-export-deps.sh`
- That script must be hooked into the bootstrap through `.env`
- If the host already has everything required, the script must still exist as a
  canonical `audit` path for codecs, preview export, and video readiness

## Upstream Validation Gate
- This dependency task may be prepared before the upstream UX gate for scheduling convenience.
- Phase `28` still consumes the validated refined shot produced by phase `27` from Task `27.2` before the main UI flow advances.
- Do not mark phase `28` ready until this dependency route and Task `28.0` and the upstream gate are both satisfied.
## Do Not Create
- Do not create a second bootstrap outside `scripts/bootstrap/apply-workstation.sh`.
- Do not create a second config source outside `.env`.
- Do not leave export-video readiness as an undocumented host assumption.
- Do not create a separate installer path outside the canonical script.

## Required Change
- Create `scripts/apps/install-blender-video-export-deps.sh` as the canonical
  audit or installer for video export readiness.
- Add declarative flags in `.env.example` for enabling this dependency path.
- Hook the script into `scripts/bootstrap/apply-workstation.sh` and
  `scripts/bootstrap/show-config.sh`.
- Audit at least: `Blender` availability, video export capability, preview or
  frame extraction path, and any required host codec integration.

## Deliverables
- `scripts/apps/install-blender-video-export-deps.sh`
- `.env.example`
- `scripts/lib/common.sh`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `docs/operations/blender.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/28.1.1-phase28-dependency-installation.md`
- `docs/operations/blender.md`
- `docs/SAD.md` only if this dependency path changes current supported
  architecture

## Verification

### Static checks
```bash
bash -n scripts/apps/install-blender-video-export-deps.sh
bash -n scripts/bootstrap/apply-workstation.sh
bash -n scripts/bootstrap/show-config.sh
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
bash scripts/apps/install-blender-video-export-deps.sh audit
BLENDER_VIDEO_EXPORT_DEPS_INSTALL=true scripts/bootstrap/show-config.sh
BLENDER_VIDEO_EXPORT_DEPS_INSTALL=true scripts/bootstrap/apply-workstation.sh audit
```

## Expected Evidence

* Expected output file: `scripts/apps/install-blender-video-export-deps.sh`
* Expected log/result file: audit or apply output for `Blender` video export
  readiness and bootstrap visibility
* Expected status: the repo has a canonical, reproducible path for base-video
  export readiness
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing bootstrap and `Blender` infrastructure is reused.
* No duplicate installer or config source is introduced.
* Video export readiness is available through a repo-supported path.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
