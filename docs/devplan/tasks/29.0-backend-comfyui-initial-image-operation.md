# Task 29.0: Backend canonico de imagen inicial en ComfyUI

## Execution Header
You are working in the existing repository. Implement only Task 29.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate image-seed pipelines, evidence roots, or raw ComfyUI
  product paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 29: Imagen inicial desde video base y referencias

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar la operacion backend canonica que genera una imagen inicial desde video
base y referencias reutilizando `ComfyUI` como engine.

## Minimal Context
El runtime `ComfyUI` ya existe, pero falta una operacion de producto para este
paso. Sin ella, la UI de phase `29` no tendria una ruta backend real.

## Scope Budget (Roo/Qwen)
- Primary surface: `backend_contract`
- Max changed files: `6`
- Max code areas: `2 modulos` (`runner/backend contract` + `tests/docs canonicos`)
- Out-of-scope guardrail: no tocar rutas UI ni reabrir otras fases; no agregar runners paralelos.
- Split trigger: si cerrar la task exige >`6` archivos o >`2` areas, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/comfyui/interface.md`
- `docs/comfyui/general-video-render-workflow.md`
- `src/openclaw_studio/runners/comfyui.py`
- `docs/devplan/tasks/28.0-backend-blender-base-video-export-operation.md`
- `docs/devplan/tasks/29.1-ui-initial-image-from-base-video.md`

## Existing Infrastructure to Reuse
- Existing base-video export output from phase `28`
- Existing asset-reference flow from phase `19`
- Existing `ComfyUI` runtime and runner contract
- Existing canonical evidence and status conventions

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `28.2` reports `pass` (hard gate).
- Reuse the validated base-video artifacts produced by phase `28`.
- The UI e2e for phase `29` must wait for this backend task to be complete.

## Do Not Create
- Do not expose the raw `ComfyUI` canvas as a substitute for this backend.
- Do not create a second evidence root outside canonical repo-owned paths.
- Do not create route-specific graph wiring outside the canonical runner surface.

## Required Change
- Extend the canonical `comfyui` runner with a product-ready initial-image
  operation.
- Accept structured product inputs and return canonical progress, artifacts,
  evidence refs, and readable errors.
- Keep the operation reusable by the UI phase and later render phases.

## Microtask Breakdown
- [ ] MT1: Acotar contrato backend y puntos de integracion exactos a tocar. files: `docs/devplan/tasks/29.0-backend-comfyui-initial-image-operation.md`, `src/openclaw_studio/runners/comfyui.py`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Implementar el slice minimo del backend sin duplicar runners/contratos. files: `src/openclaw_studio/runners/comfyui.py`, `src/openclaw_studio/implementations/builtin_flow_catalog.py`. verify: `PYTHONPATH=src python3 -m unittest tests -v`.
- [ ] MT3: Validar operacion canonica y cobertura de pruebas del flujo agregado. files: `src/openclaw_studio/implementations/builtin_flow_catalog.py`, `docs/architecture/runner-interface.md`. verify: `scripts/actions/runner-action.sh describe comfyui`.
- [ ] MT4: Actualizar docs canonicos y cerrar gate con trazabilidad revisable. files: `docs/devplan/tasks/29.0-backend-comfyui-initial-image-operation.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `src/openclaw_studio/runners/comfyui.py`
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/29.0-backend-comfyui-initial-image-operation.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests -v
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
scripts/actions/runner-action.sh list-targets comfyui operate
```

## Expected Evidence

* Expected output file: canonical initial-image artifacts
* Expected log/result file: backend run evidence and readable result payload
* Expected status: the repo can generate an initial image through a canonical backend path
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `ComfyUI`, base-video, and evidence infrastructure is reused.
* No duplicate pipeline or evidence root is introduced.
* The backend operation is sufficient for the UI phase and later shot generation.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
