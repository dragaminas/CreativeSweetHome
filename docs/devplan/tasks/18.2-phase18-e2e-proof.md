# Task 18.2: Prueba end-to-end de Phase 18

## Execution Header
You are working in the existing repository. Implement only Task 18.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical asset catalog UI.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 18: Catalogacion de assets en UI

## Status
`pending`

## Goal
Probar en una corrida real la catalogacion de assets de una escena desde la UI
hasta la persistencia de personajes y objetos con estado revisable.

## Minimal Context
La fase se cierra cuando una escena ya scaffolded puede recibir assets reales
desde la UI y dejar evidencia en manifests o rutas canonicas.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/18.1-ui-asset-catalog.md`
- `apps/openclaw-ui/`

## Existing Infrastructure to Reuse
- Existing scene scaffold flow from phase `17`
- Existing shell routes from phase `15`
- Existing manifest layout introduced by Task `18.1`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.

## Upstream Validation Gate
- Start only after Task `17.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated scene scaffold produced by phase `17`.
- Execute this proof only after the implementation tasks of phase `18` and the asset-catalog implementation of phase `18` are complete.
## Do Not Create
- Do not replace the real proof with only unit tests.
- Do not store proof artifacts outside canonical repo-owned paths.

## Required Change
- Run a real browser-backed asset catalog flow.
- Create or update at least one `Personaje` and one `Objeto`.
- Verify that the resulting state is persisted in the canonical layout.
- Publish the real outcome of the proof.

## Deliverables
- real asset-catalog evidence
- any minimal code or doc updates needed to complete the proof
- `docs/devplan/tasks/18.2-phase18-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/18.2-phase18-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- asset-catalog
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase18-asset-catalog
```

## Expected Evidence

* Expected output file: persisted asset manifests for at least one character
  and one object
* Expected log/result file: e2e report showing catalog creation or update
* Expected status: real `pass` for asset cataloging or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or persistence path is introduced.
* Real assets are cataloged and persisted through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
