# Task 23.1: Workspace UI de descripcion de tomas

## Execution Header
You are working in the existing repository. Implement only Task 23.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate shot models, storage paths, or translators.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 23: Descripcion de tomas en UI

## Status
`pending`

## Goal
Implementar el workspace que traduce una escena y sus assets listos en
`shot briefs` estructurados para animacion, composicion y render.

## Minimal Context
La fase debe apoyarse en:

- escena ya descrita
- scaffold de escena ya creado
- assets catalogados y, cuando aplique, preparados en 3D

El resultado debe ser un `shot brief` reusable por `Kimodo`, `Blender`,
`ComfyUI`, y etapas posteriores.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/16.1-ui-scene-description-workspace.md`
- `docs/devplan/tasks/18.1-ui-asset-catalog.md`
- `docs/comfyui/blender-bridge.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing scene brief flow from phase `16`
- Existing scaffold and asset catalog flows from phases `17-18`
- Existing product shell from phase `15`
- Existing translation seam established by the UI foundation

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.

## Upstream Validation Gate
- Start only after Task `22.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated character and pipeline state produced by phase `22`.
- Persist the shot brief on canonical manifest paths so Task `23.2` validates the same planning output.
## Do Not Create
- Do not create a second shot-authoring app or schema family.
- Do not persist shot state only in browser-local storage.
- Do not duplicate translation logic already created for scene description.

## Required Change
- Add the shot-description workspace to the product UI.
- Capture shot intent, framing, duration, characters, and constraints.
- Persist a structured shot brief in a repo-owned, reviewable path.
- Provide consistency checks against the scene and its assets.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/23.1-ui-shot-description-workspace.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- shot-brief
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase23-shot-brief
```

## Expected Evidence

* Expected output file: persisted shot-brief artifact in a canonical repo-owned path
* Expected log/result file: e2e evidence showing guided capture and consistency
  feedback
* Expected status: the UI can create reusable shot descriptions
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing shell and scene or asset infrastructure is reused.
* No duplicate shot model or storage path is introduced.
* The workspace produces reusable shot briefs with consistency feedback.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
