# Task 11.7: Import Blender y metricas de outputs Trellis2

## Execution Header
You are working in the existing repository. Implement only Task 11.7.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 11: Reapertura 3D en ComfyUI con Trellis2 GGUF

## Status
`pending`

## Goal
Importar en `Blender` los GLB relevantes de `Trellis2` y registrar metricas
minimas comparables: vertices, caras, texturas, tamano y exito de importacion.

## Minimal Context
Ya existe al menos un GLB Trellis2 Q4 texturizado. El gap actual no es de
preflight sino de handoff verificable a `Blender`, que ya se usa en la linea
`Hunyuan3D` y debe reutilizarse aqui.

Debe seguir siendo compatible:

- la lectura de resultados de `11.6`
- el handoff a `Blender` ya usado en `docs/hunyuan3d/validation-results.md`
- el documento unico de resultados Trellis2

Fuera de alcance:

- crear un pipeline nuevo de preview o metricas solo para Trellis2

## Files to Inspect First
- `docs/comfyui/trellis2-gguf-validation-results.md`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/hunyuan3d/validation-results.md`
- `scripts/apps/blender_render_glb_preview.py`
- `scripts/apps/blender.sh`

## Existing Infrastructure to Reuse
- Existing Blender bridge: `scripts/apps/blender.sh`
- Existing preview helper: `scripts/apps/blender_render_glb_preview.py`
- Existing metric pattern: `docs/hunyuan3d/validation-results.md`
- Existing evidence doc: `docs/comfyui/trellis2-gguf-validation-results.md`

## Do Not Create
- Do not create a separate Trellis-only Blender bridge.
- Do not create a second mesh-metrics format.
- Do not create another results document outside `docs/comfyui/trellis2-gguf-validation-results.md`.

## Required Change
- Importar los GLB Trellis2 relevantes en `Blender`.
- Registrar al menos vertices, caras, texturas detectadas, tamano y exito de importacion.
- Si hace falta un helper, extender uno existente en vez de duplicarlo.
- Volcar la evidencia en el documento de resultados ya existente.

## Deliverables
- `docs/comfyui/trellis2-gguf-validation-results.md`
- `scripts/apps/blender_render_glb_preview.py` solo si hace falta ampliarlo

## Canonical Docs to Update
- `docs/SAD.md` solo si el handoff actual a `Blender` cambia de forma relevante
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/11.7-trellis2-blender-import-metrics.md`
- `docs/devplan/archive/phase-10-hunyuan3d-summary.md` solo si la comparativa cambia la lectura del baseline 3D

## Verification

### Static checks
```bash
python3 -m compileall scripts/apps
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
scripts/apps/blender.sh status
# Reutiliza el patron de import headless ya usado en la linea Hunyuan3D para medir el GLB Trellis2.
scripts/devplan/check-task-files.sh
```

## Expected Evidence

* Expected output file: `docs/comfyui/trellis2-gguf-validation-results.md`
* Expected log/result file: metricas revisables de importacion Blender para los GLB Trellis2
* Expected status: importacion `PASS` o fallo documentado con causa concreta
* Expected exit code: `0` cuando Blender importe el GLB sin error fatal

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
