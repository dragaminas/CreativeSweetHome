# Task 11.11.1: Gate minimo de calidad para Trellis2

## Execution Header
You are working in the existing repository. Implement only Task 11.11.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 11: Reapertura 3D en ComfyUI con Trellis2 GGUF

## Status
`pending`

## Goal
Definir e implementar el gate minimo de calidad para permitir la promocion
controlada de Trellis2.

## Minimal Context
El gate minimo pedido por el plan es:

- fixture historico
- imagen creativa real
- import `Blender`

La evidencia fuente ya vive en `11.6`, `11.7` y `11.8`. El gate debe
reutilizar esa misma infraestructura y no abrir un criterio paralelo.

## Files to Inspect First
- `docs/comfyui/trellis2-gguf-validation-results.md`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/hunyuan3d/validation-results.md`
- `scripts/apps/install-trellis2-gguf.sh`
- `scripts/apps/comfyui-trellis2-gguf-validation.sh`
- `scripts/apps/blender_render_glb_preview.py`

## Existing Infrastructure to Reuse
- Existing evidence doc: `docs/comfyui/trellis2-gguf-validation-results.md`
- Existing installer and preflight: `scripts/apps/install-trellis2-gguf.sh`, `scripts/apps/comfyui-trellis2-gguf-validation.sh`
- Existing Blender evidence pattern: `docs/hunyuan3d/validation-results.md`

## Do Not Create
- Do not create a second quality-gate document.
- Do not create a second Blender metric format.
- Do not define the gate only in prose if it can be tied to existing evidence.

## Required Change
- Formalizar el gate de calidad con criterios concretos.
- Basarlo en el fixture historico, una imagen creativa real y el import a `Blender`.
- Dejar el resultado en el documento Trellis2 y reutilizar artefactos ya publicados.

## Deliverables
- `docs/comfyui/trellis2-gguf-validation-results.md`
- `docs/comfyui/trellis2-gguf-interface.md`

## Canonical Docs to Update
- `docs/SAD.md` si el gate de calidad cambia la lectura actual de `Trellis2`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/11.11.1-trellis2-quality-gate.md`
- `docs/devplan/archive/phase-10-hunyuan3d-summary.md` solo si el gate altera la lectura del baseline vigente

## Verification

### Static checks
```bash
test -f docs/comfyui/trellis2-gguf-validation-results.md
test -f docs/comfyui/trellis2-gguf-interface.md
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
bash scripts/apps/install-trellis2-gguf.sh
scripts/apps/blender.sh status
scripts/devplan/check-task-files.sh
```

## Expected Evidence

* Expected output file: `docs/comfyui/trellis2-gguf-validation-results.md`
* Expected log/result file: evidencia de fixture historico, imagen creativa e import Blender
* Expected status: gate de calidad `pass` o `fail_quality` con lectura explicita
* Expected exit code: `0` para el preflight base y evidencia suficiente para la decision

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
