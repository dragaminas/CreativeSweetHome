# Task 31.1.1: Instalacion de dependencias de Phase 31

## Execution Header
You are working in the existing repository. Implement only Task 31.1.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate installers, config sources, or bootstrap paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 31: Montaje automatizado de tomas en DaVinci Resolve

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar la ruta canonica de auditoria o instalacion para la integracion local de
`DaVinci Resolve`, su capacidad de scripting y su uso dentro del pipeline.

## Minimal Context
La fase `31` introduce una superficie nueva de dependencias externas y posibles
bloqueos reales:

- disponibilidad del binario local de `DaVinci Resolve`
- capacidad de scripting o automatizacion local
- licencias o paquetes no auto-instalables desde el repo

El repo necesita un camino canonico para auditar o documentar esos bloqueos.

## Scope Budget (Roo/Qwen)
- Primary surface: `dependency_install`
- Max changed files: `7`
- Max code areas: `2 modulos` (`installer/audit script` + `bootstrap/config/docs`)
- Out-of-scope guardrail: no implementar features de producto; solo provision/audit reproducible.
- Split trigger: si cerrar la task exige >`7` archivos o >`2` areas, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/tasks/31.1-ui-davinci-resolve-automated-assembly.md`
- `.env.example`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `scripts/lib/common.sh`
- `docs/operations/bootstrap.md`

## Existing Infrastructure to Reuse
- Existing bootstrap entrypoint: `scripts/bootstrap/apply-workstation.sh`
- Existing config source and defaults: `.env.example`, `scripts/lib/common.sh`
- Existing config visibility: `scripts/bootstrap/show-config.sh`
- Existing repo convention for app installers: `scripts/apps/`
- Existing runner and evidence conventions for long-running operations

## Dependency Provisioning
- This task must deliver a new canonical script:
  `scripts/apps/install-davinci-resolve-deps.sh`
- That script must be hooked into the bootstrap through `.env`
- If auto-install is not legally or technically viable, the script must still
  provide a canonical `audit` path that yields explicit `blocked_*` evidence

## Upstream Validation Gate
- This dependency task may be prepared before the upstream UX gate for scheduling convenience.
- Phase `31` still consumes the validated generated shot outputs produced by phase `30` from Task `30.2` before the main UI flow advances.
- Do not mark phase `31` ready until this dependency route and Task `31.0` and the upstream gate are both satisfied.
## Do Not Create
- Do not create a second bootstrap outside `scripts/bootstrap/apply-workstation.sh`.
- Do not create a second config source outside `.env`.
- Do not leave Resolve readiness only as manual prose.
- Do not hide licensing or packaging blockers instead of detecting them.

## Required Change
- Create `scripts/apps/install-davinci-resolve-deps.sh` as the canonical audit
  or installer path for local Resolve integration.
- Add declarative flags in `.env.example` for enabling this dependency path.
- Hook the script into `scripts/bootstrap/apply-workstation.sh` and
  `scripts/bootstrap/show-config.sh`.
- Audit at least: binary presence, scripting entrypoints, expected output
  locations, and any required host prerequisites.
- Emit explicit blocker status if the repo cannot auto-provision the host.

## Microtask Breakdown
- [ ] MT1: Alinear prerequisitos y contrato de provision canonico para esta fase. files: `scripts/apps/install-davinci-resolve-deps.sh`, `docs/devplan/tasks/31.1.1-phase31-dependency-installation.md`. verify: `bash -n scripts/apps/install-davinci-resolve-deps.sh`.
- [ ] MT2: Implementar o ajustar el script de instalacion/audit con salida reproducible. files: `scripts/apps/install-davinci-resolve-deps.sh`, `.env.example`. verify: `python3 -m compileall src tests`.
- [ ] MT3: Enlazar bootstrap/config y exponer el estado en rutas operativas canonicas. files: `.env.example`, `scripts/lib/common.sh`. verify: `bash scripts/apps/install-davinci-resolve-deps.sh audit`.
- [ ] MT4: Sincronizar documentacion y gate de fase con evidencia dura. files: `docs/devplan/tasks/31.1.1-phase31-dependency-installation.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `scripts/apps/install-davinci-resolve-deps.sh`
- `.env.example`
- `scripts/lib/common.sh`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `docs/operations/bootstrap.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/31.1.1-phase31-dependency-installation.md`
- `docs/operations/bootstrap.md`
- `docs/SAD.md` only if this dependency path changes current supported
  architecture

## Verification

### Static checks
```bash
bash -n scripts/apps/install-davinci-resolve-deps.sh
bash -n scripts/bootstrap/apply-workstation.sh
bash -n scripts/bootstrap/show-config.sh
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
bash scripts/apps/install-davinci-resolve-deps.sh audit
DAVINCI_RESOLVE_INSTALL=true scripts/bootstrap/show-config.sh
DAVINCI_RESOLVE_INSTALL=true scripts/bootstrap/apply-workstation.sh audit
```

## Expected Evidence

* Expected output file: `scripts/apps/install-davinci-resolve-deps.sh`
* Expected log/result file: audit or apply output for local Resolve readiness,
  including explicit blockers when applicable
* Expected status: the repo has a canonical path for Resolve integration
  readiness, even if the host is blocked
* Expected exit code: `0` for supported audit flows

## Acceptance Criteria

* The required change is implemented.
* Existing bootstrap and config infrastructure is reused.
* No duplicate installer or config source is introduced.
* Resolve readiness is available through a repo-supported path with explicit blockers.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
