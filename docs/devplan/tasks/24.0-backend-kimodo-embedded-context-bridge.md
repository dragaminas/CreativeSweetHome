# Task 24.0: Backend de embed y contexto para Kimodo

## Execution Header
You are working in the existing repository. Implement only Task 24.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate motion-authoring apps, forks, or proxy stacks.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 24: Animacion de personajes en Kimodo embebido

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar el backend bridge que permita embeber `Kimodo` con contexto de escena,
shot, personaje y rutas de salida sin reimplementar authoring de motion.

## Minimal Context
Hoy el repo solo cubre la instalacion reproducible de `Kimodo`. Falta la capa
backend que lo haga sentirse integrado al producto: proxy o embed, contexto,
estado y handoff de salidas.

## Scope Budget (Roo/Qwen)
- Primary surface: `backend_contract`
- Target changed files (soft cap): `6`
- Hard cap (must stop and split): `8`
- Max code areas: `2 modulos` (`runner/backend contract` + `tests/docs canonicos`)
- Out-of-scope guardrail: no tocar rutas UI ni reabrir otras fases; no agregar runners paralelos.
- Split trigger: si cerrar la task supera el soft cap (`6`) o expande areas no previstas, detener, abrir task hermana y replanificar antes de seguir.
- Overflow protocol: al alcanzar hard cap (`8`), detener implementacion, marcar status `blocked_scope_split_required`, registrar gap tecnico y enlazar task hermana en `docs/devplan/01-phase-index.md`.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/kimodo/installation.md`
- `docs/devplan/tasks/24.1-kimodo-embedded-shot-animation-workspace.md`
- `scripts/apps/install-kimodo.sh`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing `Kimodo` installation path from phase `12`
- Existing shell web from phase `15`
- Existing scene, asset, and shot context from phases `18` and `23`
- Existing filesystem roots under `STUDIO_DIR`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Kimodo` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `23.2` reports `pass` (hard gate).
- Reuse the validated shot brief and shot context produced by phase `23`.
- The UI e2e for phase `24` must wait for this backend task to be complete.

## Source of Truth Matrix
- Domain: `task_status`.
- Authoritative Source: `docs/devplan/tasks/24.0-backend-kimodo-embedded-context-bridge.md` (`## Status` y `Implementation Outcome` cuando aplique).
- Derived/Projection Artifacts: `docs/devplan/task-status-index.md`, `docs/devplan/01-phase-index.md`.
- Reconciliation Command: `python3 scripts/devplan/build-task-status-index.py --write`.
- Write Rule: editar primero esta task file y luego regenerar el indice de estado.
- Domain: `runtime_state`.
- Authoritative Source: `STUDIO_DIR/Scenes/<project>/<scene>/...` (briefs/manifests/catalogos).
- Derived/Projection Artifacts: `openclaw-projects/<project>/...` (proyeccion derivada reconciliada), vistas UI de navegacion, docs de fase.
- Reconciliation Command: `npm --prefix apps/openclaw-ui run test -- openclaw-projects`.
- Write Rule: escribir primero en `STUDIO_DIR`; reconciliar proyeccion derivada; nunca editar manualmente `openclaw-projects` como origen primario.
- Domain: `validation_evidence`.
- Authoritative Source: rutas de `Expected Evidence` y salidas reales de comandos en `Verification`.
- Derived/Projection Artifacts: notas de cierre, resumenes de fase y feedback de UI.
- Reconciliation Command: ejecutar todos los comandos de `Verification` de esta task sin atajos.
- Write Rule: no marcar `done` si la evidencia no existe o no es trazable a rutas canonicas.

## Implementation Contract (No-Drift)
- Primary authority boundary: runner/bridge/backend operation de la fase, sin abrir contratos paralelos.
- Explicit non-authority boundary: no crear runner duplicado, CLI paralela, ni output root alterno.
- Required inputs (existing artifacts): `Files to Inspect First` + gate upstream declarado + docs canonicos referenciados.
- Required outputs (exact paths): `Deliverables` + evidencia de `Expected Evidence`.
- Sequencing constraints: cerrar contrato minimo en backend; cablear registro/bridge canonico; publicar estado+artefactos canonicos; verificar unidad+smoke.
- Failure policy: ante bloqueo real, devolver `blocked_*`/`fail_*` con causa tecnica, comando ejecutado y ruta afectada; no inventar rutas paralelas.
- Evidence policy: cada claim de avance debe mapear a archivo/log revisable en repo o `STUDIO_DIR`.

## Detailed Execution Context (Anti-Invention)
- Canonical authority model: `STUDIO_DIR/Scenes` es autoridad operativa; `openclaw-projects` es proyeccion reconciliada.
- Mandatory guardrails: reutilizar infraestructura existente, no duplicar contratos, no abrir taxonomias de estado paralelas.
- Gate discipline: si la task define `Upstream Validation Gate`, no iniciar implementacion hasta satisfacerlo.
- Evidence discipline: la evidencia debe quedar en rutas revisables y mencionarse explicitamente en `Implementation Outcome`.
- Drift control: si aparece una necesidad fuera de alcance, detener y crear task hermana en lugar de improvisar expansion.
- Roo/Codex handoff note: ejecutar exactamente las microtasks listadas; cualquier desviacion requiere justificar impacto y actualizar esta task file antes de codificar.
- Context note: El backend debe devolver payloads trazables y consistentes con `runner-interface` y estados canonicos.

## Do Not Create
- Do not fork `Kimodo` just to make the embed possible.
- Do not create a second motion-authoring app beside the upstream tool.
- Do not expose `Kimodo` with no product context or canonical output routing.

## Required Change
- Create the backend seam that proxies or embeds `Kimodo` inside the product.
- Pass scene, shot, character, and output-path context in a canonical way.
- Persist enough state for the UI and later animation-apply phases.
- Keep the bridge reusable without tying it to one route implementation.

## Microtask Breakdown
- [x] MT1: Acotar contrato backend y puntos de integracion exactos a tocar. files: `docs/devplan/tasks/24.0-backend-kimodo-embedded-context-bridge.md`, `apps/openclaw-ui/src/lib/server/kimodo-embed.ts`. verify: `scripts/devplan/check-task-files.sh`.
- [x] MT2: Implementar el slice minimo del backend sin duplicar runners/contratos. files: `apps/openclaw-ui/src/lib/server/kimodo-embed.ts`, `apps/openclaw-ui/src/routes/workspaces/kimodo/embed/+server.ts`. verify: `npm --prefix apps/openclaw-ui run test -- kimodo-backend`.
- [x] MT3: Validar operacion canonica y cobertura de pruebas del flujo agregado. files: `apps/openclaw-ui/src/routes/workspaces/kimodo/embed/+server.ts`, `apps/openclaw-ui/src/lib/server/studio-state.ts`. verify: `bash scripts/apps/install-kimodo.sh audit`.
- [x] MT4: Actualizar docs canonicos y cerrar gate con trazabilidad revisable. files: `docs/devplan/tasks/24.0-backend-kimodo-embedded-context-bridge.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `apps/openclaw-ui/src/lib/server/kimodo-embed.ts`
- `apps/openclaw-ui/src/routes/workspaces/kimodo/embed/+server.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `docs/kimodo/installation.md`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/24.0-backend-kimodo-embedded-context-bridge.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- kimodo-backend
```

### Integration / smoke validation
```bash
bash scripts/apps/install-kimodo.sh audit
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase24-kimodo
```

## Expected Evidence

* Expected output file: canonical context handoff and state for embedded `Kimodo`
* Expected log/result file: backend bridge evidence showing launch and context
* Expected status: `Kimodo` can be embedded with product context and output routing
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `Kimodo` infrastructure is reused.
* No duplicate motion-authoring app or proxy stack is introduced.
* The backend bridge is sufficient for the UI phase and later handoffs.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.

## Implementation Outcome (2026-05-18)

* implementation status: `done`
* backend bridge created in:
  - `apps/openclaw-ui/src/lib/server/kimodo-embed.ts`
  - `apps/openclaw-ui/src/routes/workspaces/kimodo/embed/+server.ts`
* canonical context + state persistence implemented at:
  - `STUDIO_DIR/Scenes/<project>/<scene>/shots/<shot>/manifests/kimodo-embed-context.json`
  - `STUDIO_DIR/Scenes/<project>/<scene>/shots/<shot>/manifests/kimodo-animation-state.json`
* canonical output routing prepared at:
  - `STUDIO_DIR/Exports/<project>/<shot>/kimodo/{input,output,logs}/`
* backend surface delivered:
  - `POST /workspaces/kimodo/embed` para persistir contexto canonico
  - `GET /workspaces/kimodo/embed?format=json&...` para consumir el mismo payload de bridge
* TDD evidence:
  - RED: `npm --prefix apps/openclaw-ui run test -- kimodo-backend` fallo con `prepareKimodoEmbedContext is not a function`, `POST is not a function` y `GET` devolviendo solo HTML placeholder.
  - GREEN: implementado `prepareKimodoEmbedContext` + `POST` + `GET format=json` con persistencia canonica.
  - REFACTOR: endurecida visibilidad de readiness en `studio-state` para `Kimodo` y `Validation/Kimodo`.
* canonical evidence captured from tests:
  - `/tmp/openclaw-kimodo-backend-L690OL/Scenes/pilot-feature/opening-alley/shots/sh010/manifests/kimodo-embed-context.json`
  - `/tmp/openclaw-kimodo-backend-L690OL/Scenes/pilot-feature/opening-alley/shots/sh010/manifests/kimodo-animation-state.json`
* verification results:
  - `scripts/devplan/check-task-files.sh` -> `task_files_ok`
  - `npm --prefix apps/openclaw-ui run check` -> `0 errors, 0 warnings`
  - `npm --prefix apps/openclaw-ui run test -- kimodo-backend` -> `3 passed`
  - `bash scripts/apps/install-kimodo.sh audit` -> `pass` (kimodo importable, motion_correction disponible, HF gated access granted)
  - `npm --prefix apps/openclaw-ui run test:e2e -- --grep phase24-kimodo` -> `Error: No tests found` (esperado: la prueba e2e se materializa en task `24.2`)
* upstream gate:
  - Task `23.2` `done` confirmado
