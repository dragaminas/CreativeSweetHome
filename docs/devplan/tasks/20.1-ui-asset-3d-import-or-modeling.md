# Task 20.1: Importacion o modelado 3D de assets en UI

## Execution Header
You are working in the existing repository. Implement only Task 20.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate 3D pipelines, evidence roots, or orchestration paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 20: Importacion o modelado 3D de assets en UI

## Status
`pending`

## Goal
Implementar en la UI la capa de producto que lleva un asset catalogado hasta
una importacion o candidato 3D reutilizando la linea 3D vigente del repo.

## Minimal Context
El repo ya tiene una ruta 3D activa sobre `Trellis2 GGUF` dentro de `ComfyUI`
y contratos de handoff hacia `Blender`. Esta fase debe integrarla como
producto sin abrir otro pipeline ni otra raiz de evidencia.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/19.1-ui-asset-reference-images.md`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/comfyui/3d-blender-bridge.md`
- `docs/architecture/runner-interface.md`
- `apps/openclaw-ui/`
- `src/openclaw_studio/runners/comfyui.py`

## Existing Infrastructure to Reuse
- Existing asset catalog from phase `18`
- Existing reference flow from phase `19`
- Existing `Trellis2 GGUF` path from phase `11`
- Existing runner contract and evidence conventions under `Assets3D/`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` and 3D backend provisioning paths already
  covered by the repo.

## Upstream Validation Gate
- Start only after Task `19.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated asset references produced by phase `19`.
- Complete Task `20.0` before phase `20` is considered ready for e2e proof.
## Do Not Create
- Do not create a second 3D runtime beside the current repo-supported path.
- Do not create a second evidence layout outside `Assets3D/`.
- Do not expose backend-specific node complexity to the end user.

## Required Change
- Add the product UI that sends a cataloged asset through import or 3D
  generation.
- Reuse existing 3D handoff conventions under `Assets3D/<project>/<entity_id>/`.
- Present only presets, simplified inputs, status, previews, final artifacts,
  evidence, and readable quality feedback.
- Make it explicit when the result is usable, soft-pass, or only a proxy.

## Deliverables
- updates under `apps/openclaw-ui/`
- any adapter code needed to connect the UI with the active 3D path
- manifest or layout updates required by the implementation

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/20.1-ui-asset-3d-import-or-modeling.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
python3 -m compileall src tests
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- asset-3d
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase20-asset-3d
```

## Expected Evidence

* Expected output file: 3D candidate artifacts under canonical `Assets3D/`
  layout
* Expected log/result file: UI or adapter evidence showing previews, quality
  feedback, and final artifact refs
* Expected status: the UI can reach a 3D candidate through the repo-supported
  path
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing 3D infrastructure is reused.
* No duplicate 3D runtime or evidence root is introduced.
* The user-facing flow hides backend complexity while preserving evidence.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
