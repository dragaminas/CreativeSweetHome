# Task 29.1: Imagen inicial desde video base y referencias en UI

## Execution Header
You are working in the existing repository. Implement only Task 29.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate image-seed flows, raw ComfyUI product surfaces, or
  evidence roots.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 29: Imagen inicial desde video base y referencias

## Status
`pending`

## Goal
Implementar la capa de producto que genera una imagen inicial a partir de
video base y referencias sin exponer la UI general de `ComfyUI`.

## Minimal Context
La fase debe consumir:

- el video base exportado en la fase `28`
- referencias de personaje o estilo ya existentes
- la capa de traduccion de prompts definida por el shell web

La experiencia de usuario debe seguir siendo propia del producto, no del
backend.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/28.1-ui-base-video-export.md`
- `docs/comfyui/interface.md`
- `docs/comfyui/general-video-render-workflow.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/comfyui.py`

## Existing Infrastructure to Reuse
- Existing base-video export flow from phase `28`
- Existing asset-reference flow from phase `19`
- Existing product shell and translation seam from phase `15`
- Existing `ComfyUI` runtime and runner path from phase `8`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `28.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated base video produced by phase `28`.
- Complete Task `29.0` before phase `29` is considered ready for e2e proof.
## Do Not Create
- Do not expose the raw `ComfyUI` canvas as the product flow.
- Do not create a new evidence root outside canonical export or run paths.
- Do not ask the end user for backend-specific node details.

## Required Change
- Add the UI step that generates an initial image from base video and references.
- Expose only presets, simplified inputs, progress, cancelation, previews,
  final artifacts, evidence, and readable errors.
- Translate user-facing descriptions into backend-ready inputs without leaking
  graph complexity.
- Persist resulting images and refs in canonical repo-owned paths.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `apps/openclaw-ui/src/lib/server/brief-translator.ts`
- `docs/devplan/UIPlan.md`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/29.1-ui-initial-image-from-base-video.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
python3 -m compileall src tests
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- initial-image
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase29-initial-image
```

## Expected Evidence

* Expected output file: initial-image artifacts in canonical repo-owned paths
* Expected log/result file: e2e evidence showing progress, previews, and final
  image refs
* Expected status: the UI can generate an initial image while treating
  `ComfyUI` only as an engine
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing export, reference, and ComfyUI infrastructure is reused.
* No duplicate product path or evidence root is introduced.
* The UI hides backend complexity while preserving evidence and diagnostics.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
