# Task 27.2: Prueba end-to-end de Phase 27

## Execution Header
You are working in the existing repository. Implement only Task 27.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical refine flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 27: Refinamiento manual de toma en Blender

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una sesion real el paso asistido de refinamiento manual en `Blender`
desde la UI hasta la devolucion de estado y evidencia al pipeline.

## Minimal Context
La fase se cierra cuando una toma compuesta puede entrar al paso manual,
conservar contexto en la UI y devolver un resultado revisable sin romper el
seguimiento del pipeline.

## Scope Budget (Roo/Qwen)
- Primary surface: `e2e_proof`
- Max changed files: `4`
- Max code areas: `1 flujo` (`proof run + evidencia + task docs`)
- Out-of-scope guardrail: no refactor de arquitectura ni nuevos contratos; solo prueba real y evidencia.
- Split trigger: si cerrar la task exige >`4` archivos o tocar otra fase, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/27.1-ui-blender-shot-manual-refinement.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing shot-composition output from phase `26`
- Existing product shell from phase `15`
- Existing `Exports/` layout and shot state tracking

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Blender` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `26.2` reports `pass` (hard gate).
- Reuse the validated shot composition produced by phase `26`.
- Execute this proof only after the implementation tasks of phase `27` and Task `27.0` and the manual-refinement UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not publish refine evidence outside canonical export paths.

## Required Change
- Run a real browser-backed refine-assist flow.
- Verify the UI preserves context and receives refine state or updated artifact refs.
- Publish the real outcome of the proof.

## Microtask Breakdown
- [ ] MT1: Verificar prerequisitos duros y preparar input real de la corrida e2e. files: `docs/devplan/tasks/27.2-phase27-e2e-proof.md`, `real refine-assist proof evidence`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Ejecutar la corrida e2e canonica completa sin atajos con mocks. files: `real refine-assist proof evidence`, `apps/openclaw-ui/tests/e2e/shell.spec.ts`. verify: `npm --prefix apps/openclaw-ui run test:e2e -- --grep phase27-shot-refine`.
- [ ] MT3: Registrar evidencia, estado final y bloqueos explicitos (`pass` o `blocked_*`/`fail_*`). files: `docs/devplan/tasks/27.2-phase27-e2e-proof.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- real refine-assist proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/27.2-phase27-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/27.2-phase27-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- shot-refine
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase27-shot-refine
```

## Expected Evidence

* Expected output file: refine-state or updated shot artifact refs under
  canonical export paths
* Expected log/result file: e2e report showing context retention and returned
  state
* Expected status: real `pass` for refine assist or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or export path is introduced.
* A real manual-refine assist flow is exercised through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
