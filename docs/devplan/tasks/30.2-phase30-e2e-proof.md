# Task 30.2: Prueba end-to-end de Phase 30

## Execution Header
You are working in the existing repository. Implement only Task 30.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical shot-generation flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 30: Generacion de tomas con ComfyUI como engine

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real la generacion de tomas desde la UI hasta la
publicacion de artefactos, evidencia y errores legibles.

## Minimal Context
La fase se cierra cuando un shot listo puede renderizarse desde el shell web y
completar la experiencia de producto sin abrir la UI general de `ComfyUI`.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/30.1-ui-shot-generation-with-comfyui-engine.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/comfyui.py`

## Existing Infrastructure to Reuse
- Existing initial-image and base-video flows from phases `28-29`
- Existing product shell from phase `15`
- Existing `ComfyUI` runtime and runner path from phase `8`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `29.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated initial image package produced by phase `29`.
- Execute this proof only after the implementation tasks of phase `30` and Task `30.0` and the shot-generation UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not expose the raw `ComfyUI` UI as part of the proof.
- Do not publish evidence outside canonical repo-owned paths.

## Required Change
- Run a real browser-backed shot-generation flow.
- Verify previews, final artifacts, evidence, and readable error handling.
- Publish the real outcome of the proof.

## Deliverables
- real shot-generation proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/30.2-phase30-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/30.2-phase30-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- shot-generation
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase30-shot-generation
```

## Expected Evidence

* Expected output file: rendered shot artifacts in canonical repo-owned paths
* Expected log/result file: e2e report plus progress, preview, final artifact,
  and readable-error evidence
* Expected status: real `pass` for shot generation or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or product path is introduced.
* A real shot-generation flow completes through the product UI with `ComfyUI`
  hidden as an engine.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
