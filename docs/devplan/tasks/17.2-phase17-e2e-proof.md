# Task 17.2: Prueba end-to-end de Phase 17

## Execution Header
You are working in the existing repository. Implement only Task 17.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical shell.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 17: Estructura automatizada de almacenamiento de escena

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real la creacion de una estructura de escena desde la UI
hasta la aparicion de directorios y manifiestos revisables bajo `STUDIO_DIR`.

## Minimal Context
Esta fase solo queda cerrada cuando el scaffold se crea de verdad a partir de
una escena ya descrita y deja evidencia real, no solo un mock o una simulacion.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/17.1-ui-scene-storage-scaffolding.md`
- `scripts/openclaw/setup-workspace.sh`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing shell routes from phase `15`
- Existing scene brief flow from phase `16`
- Existing layout roots under `STUDIO_DIR`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.

## Upstream Validation Gate
- Start only after Task `16.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated scene brief produced by phase `16`.
- Execute this proof only after the implementation tasks of phase `17` and the storage-scaffolding implementation of phase `17` are complete.
## Do Not Create
- Do not claim success from only unit or route-isolated tests.
- Do not publish evidence outside the canonical workspace roots.

## Required Change
- Run a real browser-backed scaffold creation from the product UI.
- Verify the expected directories and manifests are created on disk.
- Publish the real result, including blockers or collisions if they occur.

## Deliverables
- real scaffold evidence under `STUDIO_DIR`
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/17.2-phase17-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/17.2-phase17-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- scene-storage
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase17-scaffold
```

## Expected Evidence

* Expected output file: created scene scaffold on disk
* Expected log/result file: e2e report plus file-path checks for the created
  layout
* Expected status: real `pass` for scene scaffold creation or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof path is introduced.
* A real scene scaffold is created and verified on disk.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.

## Proof Outcome (2026-05-15)

* real proof status: `pass`
* dependency route used:
  `bash scripts/apps/install-ui-web-deps.sh apply`
* browser-backed commands:
  - `npm --prefix apps/openclaw-ui run dev -- --host 127.0.0.1 --port 4173`
  - `npm --prefix apps/openclaw-ui run test:e2e -- --grep phase17-scaffold`
* validated surfaces:
  `/workspaces/scene`, `POST /api/briefs/scene`, `POST /api/scenes/scaffold`
* strengthened e2e evidence in
  `apps/openclaw-ui/tests/e2e/shell.spec.ts` (`phase17-scaffold`):
  - verifies `scene-storage.json`, `assets.json`, and `shot.json`
  - verifies canonical export directories under
    `STUDIO_DIR/Exports/<project>/<shot>/{blender,comfyui}/...`
* persisted scaffold evidence example:
  - `/home/eric/Studio/Scenes/phase17-proof-1778861492991/opening-alley/manifests/scene-storage.json`
  - `/home/eric/Studio/Scenes/phase17-proof-1778861492991/opening-alley/manifests/assets.json`
  - `/home/eric/Studio/Scenes/phase17-proof-1778861492991/opening-alley/shots/sh010/manifests/shot.json`
