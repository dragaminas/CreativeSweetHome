# Task 32.2: Prueba end-to-end de Phase 32

## Execution Header
You are working in the existing repository. Implement only Task 32.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical Resolve-refine flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 32: Refinamiento manual de escena en DaVinci Resolve

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una sesion real el paso asistido de refinamiento manual de escena
desde la UI hasta la devolucion de estado y evidencia al pipeline.

## Minimal Context
La fase se cierra cuando una escena ensamblada puede entrar al paso manual,
conservar contexto en la UI y devolver un resultado revisable sin romper el
seguimiento del pipeline.

## Scope Budget (Roo/Qwen)
- Primary surface: `e2e_proof`
- Max changed files: `4`
- Max code areas: `1 flujo` (`proof run + evidencia + task docs`)
- Out-of-scope guardrail: no refactor de arquitectura ni nuevos contratos; solo prueba real y evidencia.
- Split trigger: si cerrar la task exige >`4` archivos o tocar otra fase, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/32.1-ui-davinci-resolve-manual-scene-refinement.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing Resolve assembly output from phase `31`
- Existing product shell from phase `15`
- Existing scene-state tracking and export paths

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the canonical Resolve-readiness path from Task `31.1.1`.

## Upstream Validation Gate
- Start only after Task `31.2` reports `pass` (hard gate).
- Reuse the validated scene assembly state produced by phase `31`.
- Execute this proof only after the implementation tasks of phase `32` and Task `32.0` and the manual-refinement UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not publish refine evidence outside canonical repo-owned paths.

## Required Change
- Run a real browser-backed Resolve-refine assist flow.
- Verify the UI preserves context and receives refine state or updated artifact refs.
- Publish the real outcome of the proof.

## Microtask Breakdown
- [ ] MT1: Verificar prerequisitos duros y preparar input real de la corrida e2e. files: `docs/devplan/tasks/32.2-phase32-e2e-proof.md`, `real Resolve-refine proof evidence`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Ejecutar la corrida e2e canonica completa sin atajos con mocks. files: `real Resolve-refine proof evidence`, `apps/openclaw-ui/tests/e2e/shell.spec.ts`. verify: `npm --prefix apps/openclaw-ui run test:e2e -- --grep phase32-resolve-refine`.
- [ ] MT3: Registrar evidencia, estado final y bloqueos explicitos (`pass` o `blocked_*`/`fail_*`). files: `docs/devplan/tasks/32.2-phase32-e2e-proof.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- real Resolve-refine proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/32.2-phase32-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/32.2-phase32-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- resolve-refine
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase32-resolve-refine
```

## Expected Evidence

* Expected output file: refine-state or updated scene artifact refs under
  canonical repo-owned paths
* Expected log/result file: e2e report showing context retention and returned
  state
* Expected status: real `pass` for Resolve refine assist or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or evidence path is introduced.
* A real Resolve-refine assist flow is exercised through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
