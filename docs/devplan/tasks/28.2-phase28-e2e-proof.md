# Task 28.2: Prueba end-to-end de Phase 28

## Execution Header
You are working in the existing repository. Implement only Task 28.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical base-video flow.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 28: Exportacion de videos base de tomas

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar en una corrida real la exportacion de video base desde la UI hasta la
publicacion de video, frames y evidencia revisables bajo `Exports/`.

## Minimal Context
La fase se cierra cuando una toma refinada puede exportarse desde la UI y dejar
un video base usable por las fases posteriores de imagen y render.

## Scope Budget (Roo/Qwen)
- Primary surface: `e2e_proof`
- Max changed files: `4`
- Max code areas: `1 flujo` (`proof run + evidencia + task docs`)
- Out-of-scope guardrail: no refactor de arquitectura ni nuevos contratos; solo prueba real y evidencia.
- Split trigger: si cerrar la task exige >`4` archivos o tocar otra fase, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/28.1-ui-base-video-export.md`
- `docs/devplan/tasks/28.1.1-phase28-dependency-installation.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing shot refine handoff from phase `27`
- Existing export layout and `Blender` integration
- Existing dependency path from Task `28.1.1`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the canonical export-readiness path from Task `28.1.1`.

## Upstream Validation Gate
- Start only after Task `27.2` reports `pass` (hard gate).
- Reuse the validated refined shot produced by phase `27`.
- Execute this proof only after the implementation tasks of phase `28` and Task `28.0`, Task `28.1.1`, and the base-video export UI route are complete.
## Do Not Create
- Do not replace the real proof with only mocked tests.
- Do not publish base-video evidence outside canonical export paths.

## Required Change
- Run a real browser-backed base-video export flow.
- Verify resulting video, frame, and artifact refs under canonical `Exports/`
  paths.
- Publish the real outcome of the proof.

## Microtask Breakdown
- [ ] MT1: Verificar prerequisitos duros y preparar input real de la corrida e2e. files: `docs/devplan/tasks/28.2-phase28-e2e-proof.md`, `real base-video export proof evidence`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Ejecutar la corrida e2e canonica completa sin atajos con mocks. files: `real base-video export proof evidence`, `apps/openclaw-ui/tests/e2e/shell.spec.ts`. verify: `bash scripts/apps/install-blender-video-export-deps.sh audit`.
- [ ] MT3: Registrar evidencia, estado final y bloqueos explicitos (`pass` o `blocked_*`/`fail_*`). files: `docs/devplan/tasks/28.2-phase28-e2e-proof.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- real base-video export proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/28.2-phase28-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/28.2-phase28-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- base-video-export
```

### Integration / smoke validation
```bash
bash scripts/apps/install-blender-video-export-deps.sh audit
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase28-base-video
```

## Expected Evidence

* Expected output file: base video and related artifacts under canonical
  export paths
* Expected log/result file: e2e report plus artifact refs and export diagnostics
* Expected status: real `pass` for base-video export or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or export path is introduced.
* A real base-video export run is exercised through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
