# Task 30.1: Generacion de tomas en UI con ComfyUI como engine

## Execution Header
You are working in the existing repository. Implement only Task 30.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate shot-render flows, raw ComfyUI product surfaces, or
  evidence roots.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 30: Generacion de tomas con ComfyUI como engine

## Status
`pending`

## Goal
Implementar la experiencia final de generacion de tomas como una capa propia de
producto, usando `ComfyUI` solo como motor.

## Minimal Context
La fase debe tomar el paquete de shot listo y exponer solo:

- seleccion de intencion o preset
- inputs simplificados
- progreso
- cancelacion
- previews intermedios
- artefactos finales
- evidencia y errores legibles

El canvas y la UI general de `ComfyUI` quedan fuera de la experiencia primaria.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/29.1-ui-initial-image-from-base-video.md`
- `docs/comfyui/interface.md`
- `docs/comfyui/general-video-render-workflow.md`
- `docs/architecture/runner-interface.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/comfyui.py`

## Existing Infrastructure to Reuse
- Existing initial-image flow from phase `29`
- Existing product shell and translation seam from phase `15`
- Existing `ComfyUI` runtime and runner path from phase `8`
- Existing `Exports/` and run-evidence conventions from earlier phases

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `29.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated initial image package produced by phase `29`.
- Complete Task `30.0` before phase `30` is considered ready for e2e proof.
## Do Not Create
- Do not expose the raw `ComfyUI` canvas as the product flow.
- Do not create a second render-runner or evidence root.
- Do not force the user to know workflow JSONs, nodes, or backend graph details.

## Required Change
- Add the shot-generation workspace to the product shell.
- Translate user-facing shot intent into backend-ready render inputs.
- Expose progress, cancellation, previews, final artifacts, evidence, and
  readable errors as the primary UX.
- Persist resulting outputs in canonical repo-owned paths linked to the shot.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `apps/openclaw-ui/src/lib/server/brief-translator.ts`
- `docs/devplan/UIPlan.md`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/30.1-ui-shot-generation-with-comfyui-engine.md`
- `docs/SAD.md` only if this phase changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
python3 -m compileall src tests
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- shot-generation
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase30-shot-generation
```

## Expected Evidence

* Expected output file: rendered shot artifacts in canonical repo-owned paths
* Expected log/result file: e2e evidence showing progress, previews, final
  artifacts, and readable errors
* Expected status: the product UI can generate a shot while keeping `ComfyUI`
  hidden as an engine
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing shell, translation, and `ComfyUI` infrastructure is reused.
* No duplicate render runner or evidence root is introduced.
* The UI shows only product-facing controls and feedback, not backend graph complexity.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
