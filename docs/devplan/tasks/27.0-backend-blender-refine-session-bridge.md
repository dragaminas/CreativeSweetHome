# Task 27.0: Backend de sesion y retorno de estado para refine manual en Blender

## Execution Header
You are working in the existing repository. Implement only Task 27.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate refine bridges, export roots, or state models.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 27: Refinamiento manual de toma en Blender

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar el backend bridge que permita abrir o asistir una sesion de refine
manual en `Blender` y recuperar de vuelta un estado canonico para el pipeline.

## Minimal Context
El repo no tiene hoy un bridge canonico de sesion manual para refine de shot.
Sin esta capa, la fase UI `27` solo podria documentar el paso, no probarlo.

## Scope Budget (Roo/Qwen)
- Primary surface: `backend_contract`
- Max changed files: `6`
- Max code areas: `2 modulos` (`runner/backend contract` + `tests/docs canonicos`)
- Out-of-scope guardrail: no tocar rutas UI ni reabrir otras fases; no agregar runners paralelos.
- Split trigger: si cerrar la task exige >`6` archivos o >`2` areas, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/26.0-backend-blender-shot-composition-operation.md`
- `docs/operations/blender.md`
- `docs/comfyui/blender-bridge.md`
- `docs/devplan/tasks/27.1-ui-blender-shot-manual-refinement.md`

## Existing Infrastructure to Reuse
- Existing shot composition output from phase `26`
- Existing `Exports/` layout and `Blender` context
- Existing product shell conventions for long-running state

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Blender` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `26.2` reports `pass` (hard gate).
- Reuse the validated shot composition outputs under
  `Exports/<project>/<shot>/blender/`.
- The UI e2e for phase `27` must wait for this backend task to be complete.

## Do Not Create
- Do not create a fake web reimplementation of `Blender`.
- Do not create a second refine-state model outside canonical repo-owned state.
- Do not create a new export root outside `Exports/`.

## Required Change
- Define a backend bridge for opening or assisting manual refine in `Blender`.
- Capture return state, updated artifact refs, and checklist completion in
  canonical repo-owned state.
- Keep the bridge reusable by the UI phase without route-specific behavior.

## Microtask Breakdown
- [ ] MT1: Acotar contrato backend y puntos de integracion exactos a tocar. files: `docs/devplan/tasks/27.0-backend-blender-refine-session-bridge.md`, `src/openclaw_studio/runners/blender.py`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Implementar el slice minimo del backend sin duplicar runners/contratos. files: `src/openclaw_studio/runners/blender.py`, `src/openclaw_studio/application/session_engine.py`. verify: `PYTHONPATH=src python3 -m unittest tests -v`.
- [ ] MT3: Validar operacion canonica y cobertura de pruebas del flujo agregado. files: `src/openclaw_studio/application/session_engine.py`, `docs/architecture/runner-interface.md`. verify: `scripts/actions/blender-action.sh status`.
- [ ] MT4: Actualizar docs canonicos y cerrar gate con trazabilidad revisable. files: `docs/devplan/tasks/27.0-backend-blender-refine-session-bridge.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `src/openclaw_studio/runners/blender.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/27.0-backend-blender-refine-session-bridge.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests -v
```

### Integration / smoke validation
```bash
scripts/actions/blender-action.sh status
```

## Expected Evidence

* Expected output file: canonical refine-session state or updated artifact refs
* Expected log/result file: backend bridge evidence for manual refine handoff
* Expected status: the repo can track and recover manual `Blender` refine state
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `Blender` and export infrastructure is reused.
* No duplicate refine bridge or state root is introduced.
* The backend bridge is sufficient for the UI phase and later base-video export.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
