# Task 11.11.3: Promocion controlada de Trellis2 como default

## Execution Header
You are working in the existing repository. Implement only Task 11.11.3.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 11: Reapertura 3D en ComfyUI con Trellis2 GGUF

## Status
`pending`

## Goal
Promocionar `Trellis2 GGUF` como motor 3D por defecto solo cuando pasen el
gate de calidad y el gate operativo.

## Minimal Context
Hoy el catalogo de flujos 3D sigue priorizando `Hunyuan3D` y deja `SF3D` en
`legacy`. No existe todavia una variante Trellis2 integrada como opcion
seleccionable principal del catalogo.

Debe seguir siendo compatible:

- el alias de producto `UC-3D-*`
- la continuidad operativa de `Hunyuan3D`
- el contrato actual del catalogo y de `session_engine`

Fuera de alcance:

- eliminar `Hunyuan3D`
- reescribir la seleccion de variantes desde cero

## Files to Inspect First
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/comfyui/trellis2-gguf-validation-results.md`
- `docs/hunyuan3d/validation-results.md`

## Existing Infrastructure to Reuse
- Existing flow catalog: `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- Existing variant selection: `src/openclaw_studio/application/session_engine.py`
- Existing product baseline: `Hunyuan3D` variants `ADAPTABLE`
- Existing Trellis2 evidence: `docs/comfyui/trellis2-gguf-validation-results.md`

## Do Not Create
- Do not create a second flow catalog.
- Do not create a parallel selector outside `session_engine`.
- Do not remove `Hunyuan3D` as fallback-ready variant.

## Required Change
- Integrar Trellis2 como variante de catalogo solo si `11.11.1` y `11.11.2`
  estan resueltos.
- Hacer que la seleccion por defecto cambie de forma controlada y reversible.
- Documentar el cambio de baseline y su condicion de entrada.

## Deliverables
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `src/openclaw_studio/application/session_engine.py`
- `docs/comfyui/trellis2-gguf-interface.md`

## Canonical Docs to Update
- `docs/SAD.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/11.11.3-trellis2-default-promotion.md`
- `docs/devplan/archive/phase-10-hunyuan3d-summary.md` si la promocion cambia el baseline 3D vigente

## Verification

### Static checks
```bash
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests.test_flow_catalog -v
scripts/devplan/check-task-files.sh
```

### Integration / smoke validation
```bash
PYTHONPATH=src python3 -m openclaw_studio
bash scripts/apps/install-trellis2-gguf.sh
```

## Expected Evidence

* Expected output file: `docs/comfyui/trellis2-gguf-interface.md`
* Expected log/result file: catalogo actualizado y criterio de default documentado
* Expected status: Trellis2 solo se selecciona como default cuando ambos gates pasen
* Expected exit code: `0` para verificaciones de catalogo y preflight

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
