# Task 16.2: Prueba end-to-end de Phase 16

## Execution Header
You are working in the existing repository. Implement only Task 16.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof flow outside the canonical UI route.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 16: Descripcion de escena en UI

## Status
`pending`

## Goal
Probar en una corrida real la captura de una escena desde la UI hasta la
publicacion de un `scene brief` revisable.

## Minimal Context
La fase solo se considera cerrada cuando haya evidencia real de que una
persona puede:

- abrir el workspace
- describir una escena
- recibir feedback legible
- obtener un brief persistido y reusable

Los checks estaticos no sustituyen esa prueba.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/16.1-ui-scene-description-workspace.md`
- `apps/openclaw-ui/`

## Existing Infrastructure to Reuse
- Existing shell and routes from phase `15`
- Existing web dependency path from phase `15`
- Existing brief or manifest path introduced by Task `16.1`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.

## Upstream Validation Gate
- Start only after Task `15.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated shell web and shared translation seam produced by phase `15`.
- Execute this proof only after the implementation tasks of phase `16` and the shell implementation from phase `16` are complete.
## Do Not Create
- Do not replace the real proof with only unit tests.
- Do not keep the brief only in volatile browser state.

## Required Change
- Run a real browser-backed flow for scene description.
- Persist the resulting scene brief in the canonical repo-owned location.
- Publish the real outcome of the proof, including ambiguity or validation
  problems if they exist.

## Deliverables
- real e2e evidence for scene-description capture
- any minimal code or doc updates needed to make the proof succeed
- `docs/devplan/tasks/16.2-phase16-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/16.2-phase16-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- scene-brief
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase16-scene-brief
```

## Expected Evidence

* Expected output file: persisted scene-brief artifact from a real browser flow
* Expected log/result file: e2e report showing capture, validation, and save
* Expected status: real `pass` for scene-description capture or explicit
  blocker with diagnosis
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof path is introduced.
* A real scene brief is captured and persisted through the product UI.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
