# Task 27.1: Refinamiento manual de toma en Blender asistido por UI

## Execution Header
You are working in the existing repository. Implement only Task 27.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate refine workflows, status taxonomies, or export roots.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 27: Refinamiento manual de toma en Blender

## Status
`pending`

## Goal
Implementar el paso asistido de refinamiento manual en `Blender` dentro del
shell de producto sin intentar reimplementar la UI completa de `Blender`.

## Minimal Context
La composicion automatizada puede necesitar ajuste manual. Esta fase debe dar:

- contexto de shot
- checklist
- estado del pipeline
- rutas de salida

La app puede lanzar o asistir `Blender`, pero no debe simular toda su UI.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/26.1-ui-blender-shot-composition.md`
- `docs/operations/blender.md`
- `docs/comfyui/blender-bridge.md`
- `apps/openclaw-ui/`

## Existing Infrastructure to Reuse
- Existing shot-composition flow from phase `26`
- Existing `Exports/` layout and `Blender` integration
- Existing product shell from phase `15`

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Blender` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `26.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated shot composition produced by phase `26`.
- Complete Task `27.0` before phase `27` is considered ready for e2e proof.
## Do Not Create
- Do not create a fake web reimplementation of `Blender`.
- Do not create a second refine status model outside canonical repo-owned state.
- Do not create a new export root outside `Exports/`.

## Required Change
- Add a manual-refinement workspace in the product UI.
- Expose context, state, checklist, and routes to open or assist `Blender`.
- Capture the return state of the refinement step and make it visible in the UI.
- Keep the handoff into base-video export explicit and reviewable.

## Deliverables
- updates under `apps/openclaw-ui/`
- any helper code needed to track refine state and artifact refs
- documentation updates if the chosen seam changes the agreed plan

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/27.1-ui-blender-shot-manual-refinement.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- shot-refine
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase27-shot-refine
```

## Expected Evidence

* Expected output file: refine-state or updated artifact refs for the target shot
* Expected log/result file: e2e evidence showing context, checklist, and
  returned refinement state
* Expected status: the UI can assist a manual `Blender` refinement step without
  becoming a second DCC
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `Blender`, shot, and export infrastructure is reused.
* No duplicate refine workflow or export root is introduced.
* The UI provides context and state for manual refinement without reimplementing
  `Blender`.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
