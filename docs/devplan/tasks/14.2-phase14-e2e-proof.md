# Task 14.2: Prueba end-to-end de Phase 14

## Execution Header
You are working in the existing repository. Implement only Task 14.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 14: Rigging humanoide automatizado con Blender y Rigify

## Status
`blocked`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Probar de forma end-to-end la fase `14` con una corrida real del flujo
canonico de rigging humanoide local:
`dependency install -> prepared humanoid input -> create rig -> validation poses -> evidence review`

## Minimal Context
La fase `14` debe tener una tarea dedicada de prueba end-to-end porque la
metodologia del repo exige un gate explicito antes de marcar una fase como
`done`.

Esta tarea no implementa hoy el sistema de rigging; deja preparado el lugar
canonico del proof task que debera ejecutarse cuando la fase ya tenga:

- una tarea hoja cerrada para `dependency installation/audit`
- ruta canonica de instalacion o audit de dependencias de rigging
- runner o extension del runner canonico para `Create rig`
- validacion automatica de poses/deformaciones
- evidencia publicada bajo el layout canonico de `Assets3D`

Su funcion es asegurar desde ya que la fase no podra cerrarse con solo
planning o implementacion parcial sin una corrida real revisable.

## Scope Budget (Roo/Qwen)
- Primary surface: `e2e_proof`
- Target changed files (soft cap): `6`
- Hard cap (must stop and split): `8`
- Max code areas: `1 flujo` (`proof run + evidencia + task docs`)
- Out-of-scope guardrail: no refactor de arquitectura ni nuevos contratos; solo prueba real y evidencia.
- Split trigger: si cerrar la task supera el soft cap (`6`) o expande areas no previstas, detener, abrir task hermana y replanificar antes de seguir.
- Overflow protocol: al alcanzar hard cap (`8`), detener implementacion, marcar status `blocked_scope_split_required`, registrar gap tecnico y enlazar task hermana en `docs/devplan/01-phase-index.md`.

## Files to Inspect First
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/14.1-blender-rigify-humanoid-rigging-plan.md`
- `docs/devplan/tasks/14.1.1-phase14-dependency-installation.md`
- `docs/architecture/runner-interface.md`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/actions/runner-action.sh`
- `docs/comfyui/3d-blender-bridge.md`
- `docs/operations/blender.md`

## Existing Infrastructure to Reuse
- Existing phase `13` end-to-end proof pattern: `docs/devplan/tasks/13.3-phase13-e2e-proof.md`
- Existing runner contract and evidence model: `docs/architecture/runner-interface.md`
- Existing bootstrap seam for dependency installers: `scripts/bootstrap/apply-workstation.sh`
- Existing `Assets3D` layout and cleanup prerequisite from phase `13`

## Dependency Provisioning
- Esta tarea no define una ruta nueva de dependencias por si sola
- Debe reutilizar la ruta canonica que implemente
  `docs/devplan/tasks/14.1.1-phase14-dependency-installation.md`
- Si `14.1.1` no esta resuelta, esta prueba no puede declararse completa aunque
  el runner futuro exista

## Upstream Validation Gate
- Start only after Task `13.3` reports `pass` or `soft_pass_with_fallback`
  (hard gate del handoff `cleaned/remeshed`).
- Start only after Task `14.1.1` reports `done` (dependency install/audit gate).
- This proof remains `blocked` until `blender` exposes a canonical rigging
  target (`create_rig_humanoid` or equivalent) plus automated pose-validation
  evidence.

## Source of Truth Matrix
- Domain: `task_status`.
- Authoritative Source: `docs/devplan/tasks/14.2-phase14-e2e-proof.md` (`## Status` y `Implementation Outcome` cuando aplique).
- Derived/Projection Artifacts: `docs/devplan/task-status-index.md`, `docs/devplan/01-phase-index.md`.
- Reconciliation Command: `python3 scripts/devplan/build-task-status-index.py --write`.
- Write Rule: editar primero esta task file y luego regenerar el indice de estado.
- Domain: `runtime_state`.
- Authoritative Source: artefactos y manifiestos canonicos declarados en esta task (`scripts/`, `docs/operations/`, evidencia de runner).
- Derived/Projection Artifacts: resumenes de fase y estado (`docs/devplan/01-phase-index.md`, `docs/devplan/task-status-index.md`).
- Reconciliation Command: `scripts/devplan/check-task-files.sh`.
- Write Rule: actualizar primero artefacto operativo canonico; luego sincronizar documentacion y estado derivado.
- Domain: `validation_evidence`.
- Authoritative Source: rutas de `Expected Evidence` y salidas reales de comandos en `Verification`.
- Derived/Projection Artifacts: notas de cierre, resumenes de fase y feedback de UI.
- Reconciliation Command: ejecutar todos los comandos de `Verification` de esta task sin atajos.
- Write Rule: no marcar `done` si la evidencia no existe o no es trazable a rutas canonicas.

## Implementation Contract (No-Drift)
- Primary authority boundary: escenario e2e real de fase y recoleccion de evidencia en rutas canonicas.
- Explicit non-authority boundary: no reemplazar prueba real por mocks/unit-only ni publicar evidencia fuera de rutas canonicas.
- Required inputs (existing artifacts): `Files to Inspect First` + gate upstream declarado + docs canonicos referenciados.
- Required outputs (exact paths): `Deliverables` + evidencia de `Expected Evidence`.
- Sequencing constraints: preparar prerequisitos reales de fase; ejecutar flujo completo sin atajos; capturar evidencia en disco; registrar resultado con estado explicito.
- Failure policy: ante bloqueo real, devolver `blocked_*`/`fail_*` con causa tecnica, comando ejecutado y ruta afectada; no inventar rutas paralelas.
- Evidence policy: cada claim de avance debe mapear a archivo/log revisable en repo o `STUDIO_DIR`.

## Detailed Execution Context (Anti-Invention)
- Canonical authority model: la autoridad operativa esta en artefactos canonicos de scripts/runners declarados por la fase.
- Mandatory guardrails: reutilizar infraestructura existente, no duplicar contratos, no abrir taxonomias de estado paralelas.
- Gate discipline: si la task define `Upstream Validation Gate`, no iniciar implementacion hasta satisfacerlo.
- Evidence discipline: la evidencia debe quedar en rutas revisables y mencionarse explicitamente en `Implementation Outcome`.
- Drift control: si aparece una necesidad fuera de alcance, detener y crear task hermana en lugar de improvisar expansion.
- Roo/Codex handoff note: ejecutar exactamente las microtasks listadas; cualquier desviacion requiere justificar impacto y actualizar esta task file antes de codificar.
- Context note: La prueba debe ejercer flujo completo de usuario/sistema y fallar si falta evidencia o prerequisito.

## Do Not Create
- Do not create a parallel proof path outside the canonical runner and bootstrap seams.
- Do not replace the future real proof with only unit tests or mocked runs.
- Do not mark the phase `14` as complete without this proof task being executed or explicitly blocked with documented evidence.

## Required Change
- Execute the canonical dependency and runner preflight for phase `14`.
- Attempt the canonical `Create rig` path through `scripts/actions/runner-action.sh`.
- Publish the real proof outcome (`pass` or explicit `blocked_*`/`fail_*`) with
  evidence and diagnosis.
- Keep explicit that phase `14` cannot be marked `done` without this task in
  `pass`.

## Microtask Breakdown
- [ ] MT1: Verificar prerequisitos duros y preparar input real de la corrida e2e. files: `docs/devplan/tasks/14.2-phase14-e2e-proof.md`, `docs/devplan/tasks/14.2-phase14-e2e-proof.md`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Ejecutar la corrida e2e canonica completa sin atajos con mocks. files: `docs/devplan/tasks/14.2-phase14-e2e-proof.md`, `docs/devplan/01-phase-index.md`. verify: `bash scripts/apps/install-3d-rigging-deps.sh audit && scripts/actions/runner-action.sh describe blender && scripts/actions/runner-action.sh list-targets blender operate`.
- [ ] MT3: Registrar evidencia, estado final y bloqueos explicitos (`pass` o `blocked_*`/`fail_*`). files: `docs/devplan/tasks/14.2-phase14-e2e-proof.md`, `docs/devplan/00-project-invariants.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `docs/devplan/tasks/14.2-phase14-e2e-proof.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`

## Canonical Docs to Update
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/14.2-phase14-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
true
```

### Integration / smoke validation
```bash
bash scripts/apps/install-3d-rigging-deps.sh audit
scripts/actions/runner-action.sh describe blender
scripts/actions/runner-action.sh list-targets blender operate
OPENCLAW_RUNNER_INPUTS_JSON='{"prepared_model_path":"/tmp/humanoid-prepared.obj","project_id":"phase14-e2e","entity_id":"humanoid-e2e"}' \
scripts/actions/runner-action.sh start blender operate create_rig_humanoid
scripts/devplan/check-task-files.sh
```

## Expected Evidence

* Expected output file: `docs/devplan/tasks/14.2-phase14-e2e-proof.md`
* Expected log/result file: salida de `install-3d-rigging-deps.sh audit`, `runner-action.sh describe/list-targets/start`, y `scripts/devplan/check-task-files.sh`
* Expected status: `pass` si existe el target canonico de rigging y evidencia completa, o `blocked_*`/`fail_*` con causa tecnica verificable
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Phase `14` has a dedicated end-to-end proof task as an explicit gate.
* The docs reflect that the phase cannot close without this task.
* The task can be reviewed independently.

## Implementation Outcome (2026-05-17)

* implementation status: `blocked`
* proof run status: `fail_compile`
* TDD evidence:
  - RED: `scripts/actions/runner-action.sh list-targets blender operate` solo expone `cleanup_pre_rig_humanoid`; no existe `create_rig_humanoid`.
  - RED: `OPENCLAW_RUNNER_INPUTS_JSON='{"prepared_model_path":"/tmp/humanoid-prepared.obj","project_id":"phase14-e2e","entity_id":"humanoid-e2e"}' scripts/actions/runner-action.sh start blender operate create_rig_humanoid` devuelve `accepted=false`, `status=fail_compile`, `target_id desconocido`.
  - GREEN: se registra el bloqueo canonico en esta task con comandos y diagnostico revisables, sin crear rutas paralelas ni mocks.
* dependency gate evidence:
  - `bash scripts/apps/install-3d-rigging-deps.sh audit` -> `status=pass` (`Rigify`, export `glTF/FBX`, preview render en background).
* runner contract evidence:
  - `scripts/actions/runner-action.sh describe blender` -> runner `blender` operativo con `default_evidence_root=/home/eric/Studio/Assets3D`.
  - `scripts/actions/runner-action.sh list-targets blender operate` -> solo `cleanup_pre_rig_humanoid`.
* blocking diagnosis:
  - falta registrar la operacion canonica `create_rig_humanoid` en el runner `blender` y su evidencia automatica de poses/deformaciones.
* upstream gates verified:
  - Task `13.3`: `done`
  - Task `14.1.1`: `done`
