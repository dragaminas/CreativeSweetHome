# Task 19.0: Backend canonico de referencias de assets en ComfyUI

## Execution Header
You are working in the existing repository. Implement only Task 19.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, evidence roots, prompt translators, or raw
  ComfyUI product paths.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 19: Referencias de assets con ComfyUI encapsulado

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Cerrar la operacion backend canonica que permita `asset brief -> referencias de
asset` reutilizando `ComfyUI` como engine y el contrato canonico de runner.

## Minimal Context
El repo ya tenia runtime `ComfyUI`, workflows y validaciones smoke, pero le
faltaba una operacion de producto en el runner canonico para referencias de
assets. Sin esta capa backend, la e2e UI de la fase `19` quedaba apoyada en
mocks o en la UI general de `ComfyUI`, contradiciendo el objetivo de producto.

## Scope Budget (Roo/Qwen)
- Primary surface: `backend_contract`
- Target changed files (soft cap): `6`
- Hard cap (must stop and split): `8`
- Max code areas: `2 modulos` (`runner/backend contract` + `tests/docs canonicos`)
- Out-of-scope guardrail: no tocar rutas UI ni reabrir otras fases; no agregar runners paralelos.
- Split trigger: si cerrar la task supera el soft cap (`6`) o expande areas no previstas, detener, abrir task hermana y replanificar antes de seguir.
- Overflow protocol: al alcanzar hard cap (`8`), detener implementacion, marcar status `blocked_scope_split_required`, registrar gap tecnico y enlazar task hermana en `docs/devplan/01-phase-index.md`.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/comfyui/interface.md`
- `docs/comfyui/usecases.md`
- `docs/architecture/runner-interface.md`
- `src/openclaw_studio/runners/comfyui.py`
- `src/openclaw_studio/runners/registry.py`
- `docs/devplan/tasks/19.1-ui-asset-reference-images.md`

## Existing Infrastructure to Reuse
- Existing `ComfyUI` runtime and workflow library from phase `8`
- Existing runner contract and registry
- Existing asset catalog context from phase `18`
- Existing canonical evidence and status conventions

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `18.2` reports `pass` (hard gate).
- Reuse the validated asset catalog and asset identity produced by phase `18`.
- The UI e2e for phase `19` must wait for this backend task to be complete.

## Source of Truth Matrix
- Domain: `task_status`.
- Authoritative Source: `docs/devplan/tasks/19.0-backend-comfyui-asset-reference-operation.md` (`## Status` y `Implementation Outcome` cuando aplique).
- Derived/Projection Artifacts: `docs/devplan/task-status-index.md`, `docs/devplan/01-phase-index.md`.
- Reconciliation Command: `python3 scripts/devplan/build-task-status-index.py --write`.
- Write Rule: editar primero esta task file y luego regenerar el indice de estado.
- Domain: `runtime_state`.
- Authoritative Source: `STUDIO_DIR/Scenes/<project>/<scene>/...` (briefs/manifests/catalogos).
- Derived/Projection Artifacts: `openclaw-projects/<project>/...` (proyeccion derivada reconciliada), vistas UI de navegacion, docs de fase.
- Reconciliation Command: `npm --prefix apps/openclaw-ui run test -- openclaw-projects`.
- Write Rule: escribir primero en `STUDIO_DIR`; reconciliar proyeccion derivada; nunca editar manualmente `openclaw-projects` como origen primario.
- Domain: `validation_evidence`.
- Authoritative Source: rutas de `Expected Evidence` y salidas reales de comandos en `Verification`.
- Derived/Projection Artifacts: notas de cierre, resumenes de fase y feedback de UI.
- Reconciliation Command: ejecutar todos los comandos de `Verification` de esta task sin atajos.
- Write Rule: no marcar `done` si la evidencia no existe o no es trazable a rutas canonicas.

## Implementation Contract (No-Drift)
- Primary authority boundary: runner/bridge/backend operation de la fase, sin abrir contratos paralelos.
- Explicit non-authority boundary: no crear runner duplicado, CLI paralela, ni output root alterno.
- Required inputs (existing artifacts): `Files to Inspect First` + gate upstream declarado + docs canonicos referenciados.
- Required outputs (exact paths): `Deliverables` + evidencia de `Expected Evidence`.
- Sequencing constraints: cerrar contrato minimo en backend; cablear registro/bridge canonico; publicar estado+artefactos canonicos; verificar unidad+smoke.
- Failure policy: ante bloqueo real, devolver `blocked_*`/`fail_*` con causa tecnica, comando ejecutado y ruta afectada; no inventar rutas paralelas.
- Evidence policy: cada claim de avance debe mapear a archivo/log revisable en repo o `STUDIO_DIR`.

## Detailed Execution Context (Anti-Invention)
- Canonical authority model: `STUDIO_DIR/Scenes` es autoridad operativa; `openclaw-projects` es proyeccion reconciliada.
- Mandatory guardrails: reutilizar infraestructura existente, no duplicar contratos, no abrir taxonomias de estado paralelas.
- Gate discipline: si la task define `Upstream Validation Gate`, no iniciar implementacion hasta satisfacerlo.
- Evidence discipline: la evidencia debe quedar en rutas revisables y mencionarse explicitamente en `Implementation Outcome`.
- Drift control: si aparece una necesidad fuera de alcance, detener y crear task hermana en lugar de improvisar expansion.
- Roo/Codex handoff note: ejecutar exactamente las microtasks listadas; cualquier desviacion requiere justificar impacto y actualizar esta task file antes de codificar.
- Context note: El backend debe devolver payloads trazables y consistentes con `runner-interface` y estados canonicos.

## Do Not Create
- Do not create a second runner outside `src/openclaw_studio/runners/comfyui.py`.
- Do not expose the raw `ComfyUI` canvas as a substitute for this backend.
- Do not create a second evidence format for asset references.

## Required Change
- Extend the canonical `comfyui` runner with a product-ready operation for
  asset-reference generation or import orchestration.
- Accept structured product inputs rather than raw graph-specific parameters.
- Return progress, artifact refs, evidence refs, and readable errors in the
  canonical result format.
- Keep the operation reusable by the UI phase without coupling it to one route.

## Microtask Breakdown
- [x] MT1: Acotar contrato backend y puntos de integracion exactos a tocar. files: `docs/devplan/tasks/19.0-backend-comfyui-asset-reference-operation.md`, `src/openclaw_studio/runners/comfyui.py`. verify: `scripts/devplan/check-task-files.sh`.
- [x] MT2: Implementar el slice minimo del backend sin duplicar runners/contratos. files: `src/openclaw_studio/runners/comfyui.py`, `src/openclaw_studio/runners/registry.py`. verify: `PYTHONPATH=src python3 -m unittest tests -v`.
- [x] MT3: Validar operacion canonica y cobertura de pruebas del flujo agregado. files: `src/openclaw_studio/runners/registry.py`, `src/openclaw_studio/implementations/builtin_flow_catalog.py`. verify: `scripts/actions/runner-action.sh describe comfyui`.
- [x] MT4: Actualizar docs canonicos y cerrar gate con trazabilidad revisable. files: `docs/devplan/tasks/19.0-backend-comfyui-asset-reference-operation.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `src/openclaw_studio/runners/comfyui.py`
- `src/openclaw_studio/runners/registry.py`
- `src/openclaw_studio/implementations/builtin_flow_catalog.py`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/19.0-backend-comfyui-asset-reference-operation.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests -v
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
scripts/actions/runner-action.sh list-targets comfyui operate
```

## Expected Evidence

* Expected output file: canonical backend operation or target visible on the
  `comfyui` runner
* Expected log/result file: backend run evidence and readable result payload for
  asset references
* Expected status: the repo can execute the asset-reference backend path without
  exposing raw `ComfyUI`
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing `ComfyUI` and runner infrastructure is reused.
* No duplicate runner or evidence root is introduced.
* The backend operation is consumable by the UI phase and by future channels.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.

## Implementation Outcome (2026-05-17)

* implementation status: `done`
* backend operation added on `comfyui` runner: `operate`
  - target `asset-reference-import`
  - target `asset-reference-generate`
* structured inputs validated without exponer parametros de grafo:
  - `project_id`, `scene_id`, `asset_kind`, `asset_id`
  - `reference_source_paths` (import)
  - `brief_text` + `preset_id` (generate orchestration)
* canonical evidence/result publication:
  - `STUDIO_DIR/Validation/comfyui/operate/<run_id>/manifests/run.json`
  - `STUDIO_DIR/Validation/comfyui/operate/<run_id>/manifests/summary.json`
  - `STUDIO_DIR/Validation/comfyui/operate/<run_id>/evidence/summary.md`
* canonical asset-owned publication:
  - imported references under `STUDIO_DIR/Scenes/<project>/<scene>/assets/<kind>/<asset_id>/references/published/`
  - generation request under `.../references/requests/<run_id>__request.json`
* unit coverage updated:
  - `tests/test_comfyui_runner.py` now covers `operate` targets, import pass path, generate validation error, and generate orchestration record
* verification results:
  - `scripts/devplan/check-task-files.sh` -> `task_files_ok`
  - `python3 -m compileall src tests` -> `pass`
  - `scripts/actions/runner-action.sh describe comfyui` -> `pass`
  - `scripts/actions/runner-action.sh list-targets comfyui operate` -> `pass`
  - `PYTHONPATH=src python3 -m unittest tests -v` -> `NO TESTS RAN` in this repo layout
  - fallback executed: `PYTHONPATH=src python3 -m unittest discover -s tests -p 'test*.py' -v` -> `38 tests, OK`
