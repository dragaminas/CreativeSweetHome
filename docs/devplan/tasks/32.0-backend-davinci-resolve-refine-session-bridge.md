# Task 32.0: Backend de sesion y retorno de estado para refine manual en DaVinci Resolve

## Execution Header
You are working in the existing repository. Implement only Task 32.0.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate refine bridges, evidence roots, or state models.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 32: Refinamiento manual de escena en DaVinci Resolve

## Status
`pending`

## Goal
Cerrar el backend bridge que permita abrir o asistir una sesion de refine
manual en `DaVinci Resolve` y recuperar de vuelta un estado canonico para el
pipeline.

## Minimal Context
Tras el assembly automatizado, el repo sigue sin tener una capa backend para
controlar el refine manual de escena y devolver estado consistente a la UI.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/31.0-backend-davinci-resolve-assembly-bridge.md`
- `docs/devplan/tasks/32.1-ui-davinci-resolve-manual-scene-refinement.md`
- `docs/devplan/tasks/31.1.1-phase31-dependency-installation.md`

## Existing Infrastructure to Reuse
- Existing scene-assembly state from phase `31`
- Existing dependency path from Task `31.1.1`
- Existing product shell conventions for long-running state

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the canonical Resolve-readiness path from Task `31.1.1`.

## Upstream Validation Gate
- Start only after Task `31.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated scene assembly state or explicit blocked evidence from phase `31`.
- The UI e2e for phase `32` must wait for this backend task to be complete.

## Do Not Create
- Do not create a fake web reimplementation of `DaVinci Resolve`.
- Do not create a second refine-state model outside canonical repo-owned state.
- Do not create a new scene-evidence root outside the canonical pipeline.

## Required Change
- Define a backend bridge for opening or assisting manual refine in `Resolve`.
- Capture return state, updated artifact refs, and checklist completion in
  canonical repo-owned state.
- Keep the bridge reusable by the UI phase and final export without route-specific behavior.

## Deliverables
- backend bridge code for Resolve refine sessions
- canonical state or manifest adapter for refine status
- minimal docs updates describing the bridge contract

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/32.0-backend-davinci-resolve-refine-session-bridge.md`
- `docs/SAD.md` only if this backend changes current supported architecture

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
python3 -m compileall src tests
```

### Unit tests
```bash
PYTHONPATH=src python3 -m unittest tests -v
```

### Integration / smoke validation
```bash
bash scripts/apps/install-davinci-resolve-deps.sh audit
```

## Expected Evidence

* Expected output file: canonical refine-session state or updated scene artifact refs
* Expected log/result file: backend bridge evidence for manual Resolve refine handoff
* Expected status: the repo can track and recover manual `Resolve` refine state
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing Resolve assembly and dependency infrastructure is reused.
* No duplicate refine bridge or state root is introduced.
* The backend bridge is sufficient for the UI phase and later final export work.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
