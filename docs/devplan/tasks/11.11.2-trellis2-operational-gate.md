# Task 11.11.2: Gate operativo minimo para Trellis2

## Execution Header
You are working in the existing repository. Implement only Task 11.11.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 11: Reapertura 3D en ComfyUI con Trellis2 GGUF

## Status
`archived`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Formalizar el gate operativo minimo para Trellis2: `status=pass_preflight` y
ausencia de `blocked_*`.

## Minimal Context
El preflight ya existe y publica resumen `.txt` y `.json`. El gate operativo no
debe inventar otra semantica: debe apoyarse en el resumen actual y en los
codigos `0`, `10-14` del instalador.

## Files to Inspect First
- `scripts/apps/install-trellis2-gguf.sh`
- `scripts/apps/comfyui-trellis2-gguf-validation.sh`
- `docs/comfyui/trellis2-gguf-interface.md`
- `docs/comfyui/trellis2-gguf-validation-results.md`

## Existing Infrastructure to Reuse
- Existing exit codes: `scripts/apps/install-trellis2-gguf.sh`
- Existing summary fields: `scripts/apps/comfyui-trellis2-gguf-validation.sh`
- Existing status doc: `docs/comfyui/trellis2-gguf-interface.md`

## Do Not Create
- Do not create another gate script.
- Do not create another status vocabulary.
- Do not create a second summary file outside the existing preflight outputs.

## Required Change
- Codificar y documentar que el gate operativo depende de `pass_preflight` y
  ausencia de `blocked_*`.
- Reutilizar el instalador y el resumen actual para exponer esa lectura.
- Dejar el criterio listo para ser consumido por `11.11.3` y `11.11.4`.

## Deliverables
- `scripts/apps/install-trellis2-gguf.sh`
- `scripts/apps/comfyui-trellis2-gguf-validation.sh`
- `docs/comfyui/trellis2-gguf-interface.md`

## Canonical Docs to Update
- `docs/SAD.md` solo si el gate operativo cambia la lectura actual del laboratorio `Trellis2`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/11.11.2-trellis2-operational-gate.md`
- `docs/devplan/archive/phase-10-hunyuan3d-summary.md` solo si el gate cambia la lectura del baseline vigente

## Verification

### Static checks
```bash
bash -n scripts/apps/install-trellis2-gguf.sh
bash -n scripts/apps/comfyui-trellis2-gguf-validation.sh
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
bash scripts/apps/install-trellis2-gguf.sh
```

## Expected Evidence

* Expected output file: `docs/comfyui/trellis2-gguf-interface.md`
* Expected log/result file: `trellis2_gguf_validation_summary.txt` con `status=pass_preflight` o `blocked_*`
* Expected status: gate operativo explicitado y reutilizable
* Expected exit code: `0` cuando el gate operativo pase; `10-14` cuando falle

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* Verification commands pass or documented reason is provided if they cannot be run.
* Evidence is written to the expected location when applicable.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
