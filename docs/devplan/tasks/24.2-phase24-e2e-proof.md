# Task 24.2: Prueba end-to-end de Phase 24

## Execution Header
You are working in the existing repository. Implement only Task 24.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create a second proof path outside the canonical embedded workspace.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 24: Animacion de personajes en Kimodo embebido

## Status
`pending`

## Goal
Probar en una sesion real el workspace embebido de `Kimodo` con contexto de
shot y personaje dentro del producto web.

## Minimal Context
La fase solo se cierra cuando una persona puede abrir `Kimodo` desde la app,
recibir el contexto correcto, y devolver una animacion base al pipeline sin
sentir que salio del producto principal.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/24.1-kimodo-embedded-shot-animation-workspace.md`
- `docs/kimodo/installation.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing `Kimodo` install path from phase `12`
- Existing shell routes from phase `15`
- Existing shot and asset context from phases `18` and `23`

## Dependency Provisioning
- No new dependencies are introduced in this proof task.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `Kimodo` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `23.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated shot brief produced by phase `23`.
- Execute this proof only after the implementation tasks of phase `24` and Task `24.0` and the embedded `Kimodo` workspace are complete.
## Do Not Create
- Do not replace the real proof with only mocked iframe or route tests.
- Do not publish proof state outside canonical shot or asset paths.

## Required Change
- Run a real browser-backed launch of the embedded `Kimodo` workspace.
- Verify that context is passed correctly and a base animation result returns
  to the pipeline.
- Publish the real outcome of the proof.

## Deliverables
- real `Kimodo` workspace proof evidence
- `apps/openclaw-ui/tests/e2e/shell.spec.ts`
- `docs/devplan/tasks/24.2-phase24-e2e-proof.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/24.2-phase24-e2e-proof.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- kimodo-workspace
```

### Integration / smoke validation
```bash
bash scripts/apps/install-kimodo.sh audit
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase24-kimodo
```

## Expected Evidence

* Expected output file: shot-linked animation state or artifact returned from
  the embedded `Kimodo` workspace
* Expected log/result file: e2e report showing launch, context handoff, and
  saved result
* Expected status: real `pass` for the embedded `Kimodo` flow or explicit blocker
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate proof or authoring path is introduced.
* A real embedded `Kimodo` session completes with canonical context.
* The docs reflect the real proof result rather than the desired result.
* The task can be reviewed independently.
