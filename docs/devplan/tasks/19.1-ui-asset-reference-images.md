# Task 19.1: Referencias de assets en UI con ComfyUI encapsulado

## Execution Header
You are working in the existing repository. Implement only Task 19.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate reference-generation paths, raw ComfyUI UIs, or
  prompt translators per route.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 19: Referencias de assets con ComfyUI encapsulado

## Status
`done`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Implementar la capa de producto para generacion o importacion de referencias
de assets usando `ComfyUI` solo como engine.

## Minimal Context
Esta fase debe consumir el catalogo de assets y permitir dos caminos bajo la
misma UX:

- importar referencias reales
- generar referencias desde presets e inputs simplificados

La UI no debe exponer el canvas ni la UI general de `ComfyUI`.

## Scope Budget (Roo/Qwen)
- Primary surface: `ui_integration`
- Target changed files (soft cap): `6`
- Hard cap (must stop and split): `8`
- Max code areas: `2 modulos` (`ui route/workspace` + `adapter/server bridge/tests`)
- Out-of-scope guardrail: no redisenar contratos backend ni abrir pipelines fuera del flujo de esta fase.
- Split trigger: si cerrar la task supera el soft cap (`6`) o expande areas no previstas, detener, abrir task hermana y replanificar antes de seguir.
- Overflow protocol: al alcanzar hard cap (`8`), detener implementacion, marcar status `blocked_scope_split_required`, registrar gap tecnico y enlazar task hermana en `docs/devplan/01-phase-index.md`.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/18.1-ui-asset-catalog.md`
- `docs/comfyui/interface.md`
- `docs/comfyui/usecases.md`
- `docs/architecture/runner-interface.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `src/openclaw_studio/runners/comfyui.py`

## Existing Infrastructure to Reuse
- Existing product shell from phase `15`
- Existing asset catalog from phase `18`
- Existing `ComfyUI` runtime and runner path from phase `8`
- Existing runner contract and evidence conventions

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the existing `ComfyUI` provisioning path already covered by the repo.

## Upstream Validation Gate
- Start only after Task `18.2` reports `pass` (hard gate).
- Reuse the validated asset catalog produced by phase `18`.
- Complete Task `19.0` before phase `19` is considered ready for e2e proof.
## Source of Truth Matrix
- Domain: `task_status`.
- Authoritative Source: `docs/devplan/tasks/19.1-ui-asset-reference-images.md` (`## Status` y `Implementation Outcome` cuando aplique).
- Derived/Projection Artifacts: `docs/devplan/task-status-index.md`, `docs/devplan/01-phase-index.md`.
- Reconciliation Command: `python3 scripts/devplan/build-task-status-index.py --write`.
- Write Rule: editar primero esta task file y luego regenerar el indice de estado.
- Domain: `runtime_state`.
- Authoritative Source: `STUDIO_DIR/Scenes/<project>/<scene>/...` (briefs/manifests/catalogos).
- Derived/Projection Artifacts: `openclaw-projects/<project>/...` (proyeccion derivada reconciliada), vistas UI de navegacion, docs de fase.
- Reconciliation Command: `npm --prefix apps/openclaw-ui run test -- openclaw-projects`.
- Write Rule: escribir primero en `STUDIO_DIR`; reconciliar proyeccion derivada; nunca editar manualmente `openclaw-projects` como origen primario.
- Domain: `validation_evidence`.
- Authoritative Source: rutas de `Expected Evidence` y salidas reales de comandos en `Verification`.
- Derived/Projection Artifacts: notas de cierre, resumenes de fase y feedback de UI.
- Reconciliation Command: ejecutar todos los comandos de `Verification` de esta task sin atajos.
- Write Rule: no marcar `done` si la evidencia no existe o no es trazable a rutas canonicas.

## Implementation Contract (No-Drift)
- Primary authority boundary: UI route/workspace + adapter/server de la fase, reutilizando contratos existentes.
- Explicit non-authority boundary: no crear DB, root de storage nueva, ni taxonomia paralela de estado.
- Required inputs (existing artifacts): `Files to Inspect First` + gate upstream declarado + docs canonicos referenciados.
- Required outputs (exact paths): `Deliverables` + evidencia de `Expected Evidence`.
- Sequencing constraints: leer invariantes+task; validar gate upstream; implementar en archivos objetivo; persistir en rutas canonicas; verificar static+unit+smoke/e2e.
- Failure policy: ante bloqueo real, devolver `blocked_*`/`fail_*` con causa tecnica, comando ejecutado y ruta afectada; no inventar rutas paralelas.
- Evidence policy: cada claim de avance debe mapear a archivo/log revisable en repo o `STUDIO_DIR`.

## Detailed Execution Context (Anti-Invention)
- Canonical authority model: `STUDIO_DIR/Scenes` es autoridad operativa; `openclaw-projects` es proyeccion reconciliada.
- Mandatory guardrails: reutilizar infraestructura existente, no duplicar contratos, no abrir taxonomias de estado paralelas.
- Gate discipline: si la task define `Upstream Validation Gate`, no iniciar implementacion hasta satisfacerlo.
- Evidence discipline: la evidencia debe quedar en rutas revisables y mencionarse explicitamente en `Implementation Outcome`.
- Drift control: si aparece una necesidad fuera de alcance, detener y crear task hermana en lugar de improvisar expansion.
- Roo/Codex handoff note: ejecutar exactamente las microtasks listadas; cualquier desviacion requiere justificar impacto y actualizar esta task file antes de codificar.
- Context note: La UI debe exponer progreso legible, errores accionables y persistencia canonica sin esconder estado real.

## Do Not Create
- Do not expose the raw `ComfyUI` canvas as the primary user flow.
- Do not create a second status or evidence format for image-reference runs.
- Do not ask the end user for backend-specific node details.

## Required Change
- Add a product UI for reference generation or import tied to cataloged assets.
- Expose only presets, simplified inputs, progress, cancelation, previews,
  final artifacts, evidence, and readable errors.
- Translate human asset descriptions into backend-ready prompts or parameters.
- Persist imported or generated references in canonical asset-owned paths.
- Provide checkpoint feedback for accepted references and failed runs.

## Microtask Breakdown
- [x] MT1: Delimitar el slice UI exacto (entrada, estado y salida) para esta task. files: `docs/devplan/tasks/19.1-ui-asset-reference-images.md`, `apps/openclaw-ui/src/routes/workspaces/assets/+page.svelte`. verify: `scripts/devplan/check-task-files.sh`.
- [x] MT2: Implementar la integracion UI minimamente viable sin abrir rutas paralelas. files: `apps/openclaw-ui/src/routes/workspaces/assets/+page.svelte`, `apps/openclaw-ui/src/lib/server/runner-bridge.ts`, `apps/openclaw-ui/src/routes/api/assets/+server.ts`. verify: `npm --prefix apps/openclaw-ui run test -- asset-references`.
- [x] MT3: Conectar feedback operativo (progreso, artefactos, errores legibles) y persistencia canonica. files: `apps/openclaw-ui/src/lib/server/runner-bridge.ts`, `apps/openclaw-ui/src/lib/server/studio-state.ts`, `apps/openclaw-ui/src/lib/server/brief-translator.ts`. verify: `scripts/actions/runner-action.sh describe comfyui`.
- [x] MT4: Sincronizar docs y gate de fase con evidencia verificable. files: `docs/devplan/tasks/19.1-ui-asset-reference-images.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `apps/openclaw-ui/src/routes/workspaces/assets/+page.svelte`
- `apps/openclaw-ui/src/routes/workspaces/assets/+page.server.ts`
- `apps/openclaw-ui/src/routes/api/assets/+server.ts`
- `apps/openclaw-ui/src/routes/api/assets/asset-references.test.ts`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `apps/openclaw-ui/src/lib/server/brief-translator.ts`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/19.1-ui-asset-reference-images.md`
- `docs/SAD.md` only if this phase changes the current architecture already
  supported by the repo

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
python3 -m compileall src tests
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- asset-references
```

### Integration / smoke validation
```bash
scripts/actions/runner-action.sh describe comfyui
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase19-asset-references
```

## Expected Evidence

* Expected output file: imported or generated asset references in canonical
  asset-owned paths
* Expected log/result file: e2e or adapter evidence showing progress, previews,
  and final references
* Expected status: the product UI can create or import asset references while
  keeping `ComfyUI` hidden as an engine
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing shell, runner, and ComfyUI infrastructure is reused.
* No raw ComfyUI product path is introduced.
* References are persisted with readable feedback and canonical evidence.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.

## Implementation Outcome (2026-05-17)

* implementation status: `done`
* UI de referencias agregada en `/workspaces/assets` bajo flujo encapsulado:
  - modo `import` con rutas fuente simplificadas
  - modo `generate` con brief human-readable + preset de producto
  - checkpoints de progreso, estado, artefactos y ruta de evidencia
  - botones de refresh/cancel sin exponer canvas de `ComfyUI`
* bridge y contrato reforzados para consumo de producto:
  - `startAssetReferenceRun` en `runner-bridge` con target mapping canonico
  - `AssetReferenceRunSummary` y `RunnerProgressEvent` en tipos de producto
  - traductor `buildAssetReferenceBriefText` para `brief -> brief_text` backend-ready
* capa API actualizada en `/api/assets`:
  - nuevas acciones `reference_import` y `reference_generate`
  - validaciones compile-time (`assetId`, `reference_source_paths`, `brief`)
  - actualización automática de etapa `reference_image` y `stageState` según estado de run
* persistencia y evidencia canónica:
  - import publica referencias en `STUDIO_DIR/Scenes/<project>/<scene>/assets/<kind>/<asset_id>/references/published/`
  - generate registra request en `.../references/requests/<run_id>__request.json`
  - evidencia visible en `STUDIO_DIR/Validation/comfyui/operate/<run_id>/...`
* cobertura de pruebas añadida:
  - `asset-references` unit route tests
  - e2e `phase19-asset-references` en `apps/openclaw-ui/tests/e2e/shell.spec.ts`
* verification results:
  - `scripts/devplan/check-task-files.sh` -> `task_files_ok`
  - `npm --prefix apps/openclaw-ui run check` -> `pass`
  - `python3 -m compileall src tests` -> `pass`
  - `npm --prefix apps/openclaw-ui run test -- asset-references` -> `1 passed`
  - `scripts/actions/runner-action.sh describe comfyui` -> `pass`
  - `npm --prefix apps/openclaw-ui run test:e2e -- --grep phase19-asset-references` -> `1 passed`
