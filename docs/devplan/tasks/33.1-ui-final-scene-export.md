# Task 33.1: Exportacion final de escena desde la UI

## Execution Header
You are working in the existing repository. Implement only Task 33.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate final-export workflows, evidence roots, or delivery roots.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 33: Exportacion final de escena

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Implementar el paso de producto que exporta la escena final desde la integracion
de `DaVinci Resolve` y devuelve artefactos y resumen legible a la UI principal.

## Minimal Context
La fase debe cerrar el pipeline apoyandose en:

- assembly ya realizado
- refine manual ya asistido
- rutas canonicas de escena y export

No debe abrir un nuevo flujo de entrega fuera del producto principal.

## Scope Budget (Roo/Qwen)
- Primary surface: `ui_integration`
- Max changed files: `6`
- Max code areas: `2 modulos` (`ui route/workspace` + `adapter/server bridge/tests`)
- Out-of-scope guardrail: no redisenar contratos backend ni abrir pipelines fuera del flujo de esta fase.
- Split trigger: si cerrar la task exige >`6` archivos o >`2` areas, cortar y crear task hermana antes de seguir.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/31.1-ui-davinci-resolve-automated-assembly.md`
- `docs/devplan/tasks/32.1-ui-davinci-resolve-manual-scene-refinement.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing Resolve integration and state tracking from phases `31-32`
- Existing product shell from phase `15`
- Existing scene and export context from earlier phases

## Dependency Provisioning
- No new dependencies are introduced in this phase.
- Reuse the canonical web dependency path from Task `15.1.1`.
- Reuse the canonical Resolve-readiness path from Task `31.1.1`.

## Upstream Validation Gate
- Start only after Task `32.2` reports `pass` (hard gate).
- Reuse the validated scene refine state produced by phase `32`.
- Complete Task `33.0` before phase `33` is considered ready for e2e proof.
## Do Not Create
- Do not create a second final-delivery app or export root.
- Do not create a new evidence format outside canonical repo-owned state.
- Do not bypass the existing scene pipeline just for the last export step.

## Required Change
- Add the final-scene export step to the product UI.
- Surface output profile, progress, final artifacts, and readable errors.
- Persist final delivery refs and summaries in canonical repo-owned paths.
- Make the result easy to review and hand off without leaving the product shell.

## Microtask Breakdown
- [ ] MT1: Delimitar el slice UI exacto (entrada, estado y salida) para esta task. files: `docs/devplan/tasks/33.1-ui-final-scene-export.md`, `apps/openclaw-ui/src/routes/+layout.svelte`. verify: `scripts/devplan/check-task-files.sh`.
- [ ] MT2: Implementar la integracion UI minimamente viable sin abrir rutas paralelas. files: `apps/openclaw-ui/src/routes/+layout.svelte`, `apps/openclaw-ui/src/lib/server/runner-bridge.ts`. verify: `npm --prefix apps/openclaw-ui run test -- final-scene-export`.
- [ ] MT3: Conectar feedback operativo (progreso, artefactos, errores legibles) y persistencia canonica. files: `apps/openclaw-ui/src/lib/server/runner-bridge.ts`, `apps/openclaw-ui/src/lib/server/studio-state.ts`. verify: `npm --prefix apps/openclaw-ui run test:e2e -- --grep phase33-final-export`.
- [ ] MT4: Sincronizar docs y gate de fase con evidencia verificable. files: `docs/devplan/tasks/33.1-ui-final-scene-export.md`, `docs/devplan/UIPlan.md`. verify: `scripts/devplan/check-task-files.sh`.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `docs/devplan/UIPlan.md`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/33.1-ui-final-scene-export.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- final-scene-export
```

### Integration / smoke validation
```bash
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase33-final-export
```

## Expected Evidence

* Expected output file: final scene delivery refs in canonical repo-owned paths
* Expected log/result file: e2e evidence showing export progress, final output
  refs, and readable result summary
* Expected status: the UI can publish a final scene export through the
  repo-supported product flow
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing Resolve and shell infrastructure is reused.
* No duplicate final-export workflow or evidence root is introduced.
* The UI surfaces final delivery outputs and readable summaries.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
