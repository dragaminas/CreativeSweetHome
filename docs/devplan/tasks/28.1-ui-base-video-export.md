# Task 28.1: Exportacion UI de videos base de tomas

## Execution Header
You are working in the existing repository. Implement only Task 28.1.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate export paths, status taxonomies, or video roots.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 28: Exportacion de videos base de tomas

## Status
`pending`


## Canonical Task Index Reference
- `docs/devplan/task-status-index.md`

## Goal
Implementar la capa de producto que exporta videos base de tomas desde
`Blender` y los deja listos para fases posteriores de imagen y render.

## Minimal Context
La fase debe apoyarse en la salida del shot refinado y publicar video base,
frames clave y evidencia bajo `Exports/<project>/<shot>/blender/`.

El paso no debe depender de un conocimiento manual oculto de codecs o de
configuracion de export.

## Files to Inspect First
- `docs/devplan/UIPlan.md`
- `docs/devplan/tasks/27.1-ui-blender-shot-manual-refinement.md`
- `docs/comfyui/blender-bridge.md`
- `docs/operations/blender.md`
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`

## Existing Infrastructure to Reuse
- Existing shot refine flow from phase `27`
- Existing `Exports/` layout and `Blender -> ComfyUI` bridge
- Existing product shell from phase `15`

## Planned Dependency Path
- This phase depends on a dedicated dependency leaf task:
  `docs/devplan/tasks/28.1.1-phase28-dependency-installation.md`
- That task must deliver the canonical repo script
  `scripts/apps/install-blender-video-export-deps.sh`
- Reuse the existing `Blender` install path already covered by the repo; do not
  create a second host installer just for video export

## Upstream Validation Gate
- Start only after Task `27.2` reports `pass` or `soft_pass_with_fallback`.
- Reuse the validated refined shot produced by phase `27`.
- Complete Task `28.0` and Task `28.1.1` before phase `28` is considered ready for e2e proof.
## Do Not Create
- Do not create a second base-video export workflow outside the canonical path.
- Do not create a new output root outside `Exports/`.
- Do not leave codec or preview requirements only as manual prose.

## Required Change
- Add the product UI step that exports base video for a shot.
- Surface frame range, output profile, progress, and readable failure modes.
- Publish video base, key frames, and artifact refs under canonical shot paths.
- Make the export step reusable by later image and shot-generation phases.

## Deliverables
- `apps/openclaw-ui/src/routes/+layout.svelte`
- `apps/openclaw-ui/src/lib/server/runner-bridge.ts`
- `apps/openclaw-ui/src/lib/server/studio-state.ts`
- `apps/openclaw-ui/src/lib/types/product.ts`
- `docs/devplan/UIPlan.md`
- `docs/architecture/runner-interface.md`

## Canonical Docs to Update
- `docs/devplan/UIPlan.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/28.1-ui-base-video-export.md`

## Verification

### Static checks
```bash
scripts/devplan/check-task-files.sh
npm --prefix apps/openclaw-ui run check
```

### Unit tests
```bash
npm --prefix apps/openclaw-ui run test -- base-video-export
```

### Integration / smoke validation
```bash
bash scripts/apps/install-blender-video-export-deps.sh audit
npm --prefix apps/openclaw-ui run test:e2e -- --grep phase28-base-video
```

## Expected Evidence

* Expected output file: base video and related shot artifacts under canonical
  export paths
* Expected log/result file: e2e evidence showing export progress, artifact refs,
  and readable export diagnostics
* Expected status: the UI can export a base shot video through the
  repo-supported path
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing shot and export infrastructure is reused.
* No duplicate export path or evidence root is introduced.
* The export step becomes reusable by later `ComfyUI`-driven phases.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
