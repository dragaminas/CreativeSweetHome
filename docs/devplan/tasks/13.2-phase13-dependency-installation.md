# Task 13.2: Instalacion de dependencias de Phase 13

## Execution Header
You are working in the existing repository. Implement only Task 13.2.

Before editing:
- Inspect the files listed in "Files to inspect first".
- Reuse existing patterns and infrastructure.
- Do not create duplicate runners, contracts, manifests, status formats, evidence paths, services, routes, helpers, or validation logic.
- Keep the change focused.
- Add or update the required tests or validation.
- Do not perform unrelated refactors.

## Phase
Phase 13: Cleanup 3D pre-rigging con Blender e Instant Meshes

## Status
`done`

## Goal
Cerrar el gap de instalacion del host para la fase `13`, de modo que las
dependencias reales del flujo `OpenClaw -> Blender cleanup -> Instant Meshes`
puedan provisionarse desde el repo y engancharse al bootstrap central.

## Minimal Context
La task `13.1` ya dejo listo el runner canonico y el flujo operativo de
cleanup pre-rigging, pero todavia faltaba una ruta declarativa para provisionar
las dependencias de host necesarias para usarlo en una maquina Linux nueva.

El objetivo no es reimplementar el runner ni crear otro bridge, sino dejar una
ruta reproducible de instalacion y audit para:

- `Blender`
- `Instant Meshes`
- los prerequisitos de build o runtime del host cuando hagan falta

## Files to Inspect First
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `scripts/lib/common.sh`
- `.env.example`
- `scripts/bootstrap/show-config.sh`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/apps/blender.sh`
- `scripts/apps/instant-meshes.sh`
- `docs/operations/bootstrap.md`
- `docs/operations/3d-pre-rig-cleanup.md`

## Existing Infrastructure to Reuse
- Existing bootstrap entrypoint: `scripts/bootstrap/apply-workstation.sh`
- Existing config source and defaults: `.env.example`, `scripts/lib/common.sh`
- Existing config visibility: `scripts/bootstrap/show-config.sh`
- Existing installer pattern: `scripts/apps/install-comfyui.sh`, `scripts/apps/install-kimodo.sh`
- Existing runtime wrappers: `scripts/apps/blender.sh`, `scripts/apps/instant-meshes.sh`

## Do Not Create
- Do not create a second workstation bootstrap outside `scripts/bootstrap/apply-workstation.sh`.
- Do not create a second config source outside `.env`.
- Do not create a parallel manual checklist as the supported installation path.
- Do not replace the canonical runner or wrapper entrypoints with installer-specific CLIs.

## Required Change
- Add a canonical installer script for the dependencies of phase `13`.
- Ensure the script can audit and apply in the same style as the other repo installers.
- Hook the script into the central bootstrap through declarative `.env` flags.
- Ensure the installed `Instant Meshes` location is discoverable by the existing wrapper.
- Publish a cheap smoke/audit path for the installed dependencies whenever possible.

## Deliverables
- `scripts/apps/install-3d-pre-rig-deps.sh`
- `scripts/bootstrap/apply-workstation.sh`
- `scripts/bootstrap/show-config.sh`
- `scripts/lib/common.sh`
- `.env.example`
- `docs/operations/bootstrap.md`
- `docs/operations/3d-pre-rig-cleanup.md`

## Canonical Docs to Update
- `docs/SAD.md`
- `docs/devplan/00-project-invariants.md`
- `docs/devplan/01-phase-index.md`
- `docs/devplan/feature-map.md`
- `docs/devplan/tasks/13.2-phase13-dependency-installation.md`
- `docs/operations/bootstrap.md`
- `docs/operations/3d-pre-rig-cleanup.md`

## Verification

### Static checks
```bash
bash -n scripts/apps/install-3d-pre-rig-deps.sh
bash -n scripts/bootstrap/apply-workstation.sh
bash -n scripts/bootstrap/show-config.sh
bash -n scripts/apps/instant-meshes.sh
scripts/devplan/check-task-files.sh
```

### Unit tests
```bash
python3 -m compileall src tests
```

### Integration / smoke validation
```bash
bash scripts/apps/install-3d-pre-rig-deps.sh audit
PRE_RIG_3D_DEPS_INSTALL=true scripts/bootstrap/show-config.sh
PRE_RIG_3D_DEPS_INSTALL=true scripts/bootstrap/apply-workstation.sh audit
```

## Expected Evidence

* Expected output file: `scripts/apps/install-3d-pre-rig-deps.sh`
* Expected log/result file: salida revisable de `install-3d-pre-rig-deps.sh audit` y del bootstrap en modo `audit`
* Expected status: las dependencias de phase `13` tienen una ruta canonica de instalacion y smoke/audit desde el repo
* Expected exit code: `0`

## Acceptance Criteria

* The required change is implemented.
* Existing infrastructure is reused.
* No duplicate infrastructure is introduced.
* The dependencies of phase `13` can be audited or installed from the repo.
* The central bootstrap can orchestrate the dependency script through `.env`.
* Verification commands pass or documented reason is provided if they cannot be run.
* Documentation reflects actual behavior.
* The task can be reviewed independently.
